/* ============================================================
   MeenLearn · data.js
   Subject metadata + the AI knowledge base.
   All lesson content lives in lessons-*.js files.
   Everything runs in the browser.
   ============================================================ */
window.MEEN = {
  order: ["programming", "terminal", "html", "css", "javascript", "python", "sql", "web", "git", "projects"],

  /* apps a lesson may require — shown as a "YOU NEED FIRST" bar
     on the lesson. name / url / note; lessons tag needs: ["key"] (required)
     or opt: ["key"] (recommended). */
  apps: {
    vscode: { name: "VS Code", url: "https://code.visualstudio.com", note: "free code editor — recommended for every track" },
    browser: { name: "A web browser", url: "", note: "Chrome, Firefox, Safari or Edge — you already have it" },
    devtools: { name: "DevTools", url: "", note: "built into every browser — press F12 or right-click → Inspect" },
    python: { name: "Python", url: "https://www.python.org/downloads/", note: "check “Add Python to PATH” on Windows — or use any free online Python editor instead" },
    git: { name: "Git", url: "https://git-scm.com/downloads", note: "free — verify with git --version in your terminal" },
    terminal: { name: "Your terminal", url: "", note: "built into your computer — Terminal (macOS), PowerShell (Windows), or any terminal (Linux)" },
    github: { name: "A GitHub account", url: "https://github.com/signup", note: "free — needed for remotes, Pages and hosting" },
    sqlbox: { name: "An online SQL sandbox", url: "https://www.db-fiddle.com", note: "free, no install — pick SQLite in the top-right menu" },
    node: { name: "Node.js", url: "https://nodejs.org", note: "JavaScript outside the browser — verify with node --version" },
    netlify: { name: "Netlify", url: "https://netlify.com", note: "free hosting — you can sign up with GitHub" }
  },

  subjects: {
    programming: {
      name: "Programming",
      color: "#C26DDB",
      icon: "{ }",
      page: "programming.html",
      tagline: "The way computers think",
      desc: "Algorithms, data, logic, functions and debugging — before you touch any language.",
      level: "START HERE · NO PREREQUISITES",
      blurb: "Learn to think in steps, data and decisions. Every language on this site is just this thinking written in different words."
    },
    terminal: {
      name: "Terminal",
      color: "#E86FA4",
      icon: "$",
      page: "terminal.html",
      tagline: "Control your computer by typing",
      desc: "The command line: navigate files, install tools, and run your code — the skill behind every developer tool.",
      level: "TOOL SKILL · 20 LESSONS",
      blurb: "Stop clicking, start typing. Learn the commands every developer uses daily — from your first cd to running real projects."
    },
    html: {
      name: "HTML",
      color: "#E2664F",
      icon: "</>",
      page: "html.html",
      tagline: "Structure on the web",
      desc: "Headings, links, images, forms and semantic layout — the skeleton of every page on the internet.",
      level: "BEGINNER · 20 LESSONS",
      blurb: "Build the skeleton of real pages: a full anatomy of elements, the difference between head and body, and a personal page project."
    },
    css: {
      name: "CSS",
      color: "#5E8CE6",
      icon: "#",
      page: "css.html",
      tagline: "How pages look",
      desc: "Colors, the box model, flexbox, grid, responsive design and animation — from flat text to polished layouts.",
      level: "BEGINNER · 20 LESSONS",
      blurb: "Turn structure into design. Master the box model, flexbox, grid, responsive layouts and smooth motion."
    },
    javascript: {
      name: "JavaScript",
      color: "#D8A63D",
      icon: "JS",
      page: "javascript.html",
      tagline: "Pages that respond",
      desc: "Variables, functions, the DOM, events and data — the language that makes the web interactive.",
      level: "CORE SKILL · 21 LESSONS",
      blurb: "Make pages react to people. Learn the language, then use it on real pages: events, the DOM, storage and a full to-do app."
    },
    python: {
      name: "Python",
      color: "#55A873",
      icon: "PY",
      page: "python.html",
      tagline: "Code that reads like English",
      desc: "The clearest first language: variables, logic, data, files and real mini-projects you run on your machine.",
      level: "CORE SKILL · 26 LESSONS",
      blurb: "Program outside the browser. Clean, readable code, real files, and two mini-projects: a guessing game and a contact book."
    },
    sql: {
      name: "SQL",
      color: "#9A78D0",
      icon: "SQL",
      page: "sql.html",
      tagline: "Talking to data",
      desc: "Tables, queries, joins and keys — the language every database on earth understands, in plain English.",
      level: "DATA SKILL · 20 LESSONS",
      blurb: "Ask databases questions. From your first SELECT to joins, keys and a full shop-analysis project."
    },
    web: {
      name: "Web & Hosting",
      color: "#45C4E5",
      icon: "WEB",
      page: "web.html",
      tagline: "How the web actually works",
      desc: "Browsers, servers, HTTP, DNS, and deploying your work live — from “works on my machine” to “it's on the internet”.",
      level: "SHIP IT · 20 LESSONS",
      blurb: "Understand the machine behind the pages, then publish real work: GitHub Pages, Netlify, custom domains, and your first live site."
    },
    git: {
      name: "Git",
      color: "#E07A4F",
      icon: "GIT",
      page: "git.html",
      tagline: "Never lose your work",
      desc: "Commits, branches, merges, GitHub and teamwork — the professional workflow behind every software project.",
      level: "PROFESSIONAL · 24 LESSONS",
      blurb: "Work like the industry does: version everything, branch safely, merge without fear, ship to GitHub."
    },
    projects: {
      name: "Projects",
      color: "#E94F64",
      icon: "⚒",
      page: "projects.html",
      tagline: "Build real things, explained step by step",
      desc: "Twelve complete projects — quiz games, trackers, kanban boards, API apps — each built and explained the way the best tutorials teach it.",
      level: "BUILD IT · 12 PROJECTS",
      blurb: "The skills you've learned, assembled into real applications you can show people. Every project has working code, a live demo, and a challenge."
    }
  },

  /* ---------------- BUILT-IN AI TUTOR ----------------
     A keyword-scored knowledge base over the site's own lessons.
     Each entry: k = keywords (lowercase), a = answer. */
  ai: {
    name: "MeenLearn Tutor",
    greeting: "Hi — I'm the MeenLearn tutor. I know all 10 tracks, all 203 lessons, inside out. Ask me about any concept, e.g. \"explain the CSS box model\", \"what is a primary key?\" or \"what do I need to install?\"",
    suggest: ["How do I start?", "What do I need to install?", "What is a variable?", "Explain CSS Grid", "How does the web work?", "What is a JOIN?"],
    fallback: "That one is not in my lesson library yet — I answer from the 203 MeenLearn lessons, and I'm strongest on specifics. Try \"explain flexbox\", \"f-strings\", \"git merge\", or ask about a track: programming, HTML, CSS, JavaScript, Python, SQL, Web & Hosting, Git or Projects. The lessons in the sidebar are the fastest path for the rest.",
    kb: [
      /* ---------- meta / navigation ---------- */
      { k: ["start", "begin", "new", "first", "where", "start?", "how do i start"], a: "Start with the Programming track — it teaches thinking in steps, data and decisions without any language. Then go HTML → CSS → JavaScript to build web pages. The Terminal track pairs well with any point (it's just a tool most tracks assume), Python/SQL add depth and data, Web & Hosting puts your work on the internet, Git is the professional workflow, and Projects is where you build real things and prove it. Every track has about 20 short lessons, a practical task and a challenge." },
      { k: ["meenlearn", "what is this", "site", "website", "meen", "about"], a: "MeenLearn is a free learning site by MeenDev. Ten tracks (Programming, Terminal, HTML, CSS, JavaScript, Python, SQL, Web & Hosting, Git, Projects), 203 lessons, each with an example you can see run, a practical task, a common mistake, a pro tip and a challenge. Lessons that need software show a YOU NEED bar with install links. The playground lets you write and run code instantly." },
      { k: ["playground", "try", "practice", "experiment", "run code"], a: "Open the Playground from the top menu: write HTML, CSS and JavaScript in three tabs and hit Run — everything renders live in the preview. Use the starter buttons (Blank, Click counter, Product card, Simple form) to load examples to modify." },
      { k: ["quiz", "test", "check", "score"], a: "Every track ends with a Knowledge Check: five random questions drawn from a bank of eight, with instant feedback and a saved best score. Aim for 4/5 or more before moving to the next track." },
      { k: ["progress", "track my", "remember", "saved", "continue"], a: "Your progress saves in this browser automatically: mark lessons done with the \"Mark done\" button on each lesson, and the sidebar progress bar fills up. Your best quiz score is saved too." },
      { k: ["ai", "tutor", "assistant", "bot", "ask"], a: "I'm the MeenLearn tutor, built right into the site. I know every lesson across all 10 tracks, so ask about any concept, or a subject + topic, e.g. \"explain GROUP BY\". The AI Tutor page in the top menu gives me room to think — saved history, learning goals and your stats." },
      { k: ["api", "key", "anthropic", "claude"], a: "Nothing to set up — I'm built into MeenLearn and I learn from the site's own 203 lessons. No account, no key, no setup at all. That's why I work anywhere, even on a flaky connection." },
      /* ---------- phase 3 additions (projects track + deeper python/git) ---------- */
      { k: ["projects", "project", "build a project", "what should i build", "portfolio", "practice projects", "real projects"], a: "The Projects track is 12 complete build-alongs: quiz game, password generator, color palette, filterable to-do, expense tracker (localStorage), live form validation, countdown timer, drag & drop kanban, recipe finder (API), currency converter, pricing cards, and a custom 404. Each has working code, a live demo, and a challenge — built the way the best tutorials explain: step by step, with the reason for each decision. It's the track where the skills become things you can show people." },
      { k: ["quiz game", "quiz app", "interactive quiz"], a: "A quiz is the core web loop in miniature: questions in an array, one shown at a time, click → score updates → next question, final score + restart. The Project 1 lesson walks it completely, including the two classic bugs: forgetting to lock the buttons after an answer (double-tap = double score) and not clearing a timer." },
      { k: ["kanban", "trello", "drag and drop", "drag & drop", "draggable"], a: "The HTML5 drag & drop API is three events: the card is draggable and notes itself on dragstart; each column's dragover calls preventDefault() (WITHOUT it the browser refuses the drop — the famous missing line); drop moves the card. Project 8 builds a full board with that plus localStorage. Trello is this pattern plus accounts." },
      { k: ["expense tracker", "localstorage", "local storage", "persist", "remember between visits"], a: "Persistence is two lines: localStorage.setItem(key, JSON.stringify(data)) after every change, and JSON.parse(localStorage.getItem(key)) on start. Project 5 (expense tracker) is the full pattern: load → add/delete → save → re-render, wrapped in try/catch because a corrupt notebook should mean 'start fresh', not 'crash on open'." },
      { k: ["form validation", "validate form", "registration form", "live validation"], a: "Live form validation = one rule function per field, run on every input event, painting green/red + a human message. The sneaky rule is confirm-password: it must re-check BOTH fields whenever either changes. And the submit handler re-runs every rule — never trust the paint job alone. Project 6 builds it." },
      { k: ["countdown", "countdown timer", "coming soon", "time left"], a: "A countdown is (target − now) in milliseconds, converted with ÷ and % into days/hours/min/sec, repainted by a setInterval every 1000ms — and the interval must be cleared when it hits zero (or you'll render minus days). Project 7 builds one; the programming track's ÷/% is the math under it." },
      { k: ["password generator", "strength meter", "random password"], a: "Pick from character pools (lower/upper/digits/symbols) based on the ticked boxes, build the string with random indices, and score it by length + variety — one number driving the colored bar. The lesson: data in (score), UI out (bar color/width). Project 2 builds it, and the challenge makes it enforce 'at least one of each ticked pool'." },
      { k: ["404", "page not found", "custom 404", "error page"], a: "A 404's ONE job is egress: a clear way back home. Everything else — the big outlined number, the float animation, the joke — is garnish on top of that. Project 12 builds one with flexbox centering, @keyframes, and a hover button; drop a 404.html in your GitHub Pages repo and it serves automatically." },
      { k: ["pricing cards", "pricing page", "saaS pricing", "monthly yearly toggle"], a: "Three cards in flexbox, one emphasized (border + shadow + scale + badge), hover lift — all CSS. The monthly/yearly toggle is the only JS: keep the PLANS data and re-render, so adding a fourth plan is one array entry, not a refactor. Project 11 is the CSS-range reminder: styling is not logic." },
      { k: ["currency converter", "exchange rate", "convert currency", "naira to dollar"], a: "Conversion is multiplication via a BASE currency: amount ÷ rate(from) × rate(to) — one table of 180 rates covers every pair. 'Real-time' just means the table is fetched from an API instead of hard-coded (Project 10 uses open.er-api.com, no key). The math never changes; only where the numbers come from." },
      { k: ["guardian pattern", "check before", "always check"], a: "The guardian pattern (from Python for Everybody): test the condition BEFORE acting on it — check the divisor isn't zero, the file exists, the list isn't empty. One line of checking turns a crash into a polite message. Python lesson 8 makes it a habit: write the check first, then the action." },
      { k: ["word frequency", "count words", "frequency", "most common word", "word cloud"], a: "The two-line counter: counts[word] = counts.get(word, 0) + 1 — the dictionary tallies, max(counts, key=counts.get) finds the winner. Normalize first (lowercase + strip punctuation) or 'The' and 'the' are two words. Python lesson 13 runs it on real text; it's the algorithm inside every 'top X' and word-cloud tool." },
      { k: ["sort", "sorting", "sorted", "key function", "sort by value"], a: "sorted() makes a new list, .sort() rearranges in place (and returns None — the classic 'where's my list?'). The power is key=: a function that says 'sort by THIS part'. The tuple trick sorts a dictionary by its VALUES: sorted(d.items(), key=lambda kv: kv[1]). Python lesson 15 — 'cheapest first' and 'top 5' become one-liners." },
      { k: ["regex", "regular expression", "pattern matching", "findall", "search text"], a: "Regex describes the SHAPE of text instead of a literal: \\d = a digit, + = one or more, [...] = one of these, ^/$ = start/end. re.search finds the first match, re.findall all of them; r\"...\" keeps the backslashes. Five symbols get you phone numbers, emails, dates and prices out of any text — Python lesson 17." },
      { k: ["class", "classes", "object", "oop", "inheritance", "self", "methods", "__init__"], a: "A class is a blueprint (what an object HAS + what it CAN DO); an object is a house built from it; self = 'this particular object'. Inheritance — class Savings(BankAccount) — starts with everything the parent had and adds one thing. Python lesson 22: the moment your dictionary + loose functions gets awkward, that's the smell that says 'this should be a class'." },
      { k: ["urllib", "python web", "python api", "read the web", "python json"], a: "urllib.request.urlopen(url) reads a web page/API as text; json.loads turns that text into dictionaries. It's the exact fetch → JSON → data flow of the Web track, in Python's house — Python lesson 24 builds a Lagos weather reporter from a free API, with try/except for offline days." },
      { k: ["git diff", "diff", "what changed", "see changes", "review before commit"], a: "git diff shows the exact line-by-line difference: + lines added, − lines removed, @@ where. git diff --staged shows staged vs last commit; git diff <a> <b> compares any two moments. The habit: read your own diff before every commit — the 10-second check that catches the edit you didn't mean. Git lesson 8." },
      { k: ["git rm", "rm cached", "untrack", "stop tracking", "remove from git"], a: "Two different verbs: git rm deletes the file AND stages the removal; git rm --cached stops tracking but KEEPS the file on disk — the precise tool for 'I committed a .env by mistake' (pair it with .gitignore + commit). Git lesson 14. Caveat: untracking doesn't erase history — a real leaked secret still needs rotating." },
      { k: ["time travel", "check out old commit", "detached head", "past commits", "git bisect", "debug between commits"], a: "git checkout <commit-id> opens your project AS IT WAS at that moment — 'detached HEAD' just means you're standing in a snapshot, not on a branch. Read it, git diff between moments, then git switch main to come home. Git lesson 19. Bonus: 'worked Monday, broken Wednesday?' = binary-search the commits — that's exactly what the real git bisect command automates." },
      { k: ["career", "career path", "path", "roadmap", "frontend developer", "job ready", "what order", "what should i learn", "what next", "learning order"], a: "Here's the map. Frontend developer: Programming → Terminal → HTML → CSS → JavaScript → Projects → Web & Hosting. Python + data: Programming → Python → SQL → Projects (the expense tracker is your data pipeline). Ship it: Terminal → Git → Web & Hosting (GitHub Pages first, then custom domains). The order matters less than the habit: every lesson has a task — do the task. And the Projects track is where it all becomes proof you can build things." },
      { k: ["ai dashboard", "tutor dashboard", "ask ai page", "chat history", "ai tutor"], a: "Open the AI Tutor page from the top menu (ai.html) — that is my full room: your conversation history saved in the browser, per-track topic chips, a learning-goals picker, and stats on your progress and best quiz scores. No account, no setup — everything is built in, and your chat history stays on your device." },


      /* ---------- apps & setup (what to install) ---------- */
      { k: ["what do i need", "what do i need to install", "apps", "software", "install", "download", "setup", "tools", "requirements", "prerequisite"], a: "The site tells you per lesson: any lesson that needs software shows a YOU NEED FIRST bar at the top, with a Get it link. The big ones: VS Code (free editor, recommended for everything), Python from python.org (or use a free online Python editor — zero installs), Git from git-scm.com, an online SQL sandbox (no install at all), Node.js only for the web-backend lessons, and a free GitHub account for hosting. You can start every track today — most lessons need nothing but a browser." },
      { k: ["vscode", "visual studio code", "code editor", "which editor"], a: "VS Code (free, code.visualstudio.com) is the editor this site recommends: it colors code, suggests completions, has a built-in terminal, and a Git UI. Two extensions worth adding: Python (for the Python track) and Live Server (for HTML/CSS — right-click a page → 'Open with Live Server' and the page auto-refreshes as you type). Any editor works — VS Code is just the smoothest free option." },
      { k: ["terminal track", "command line", "cmd", "powershell", "bash", "zsh", "shell"], a: "The Terminal track (20 lessons) teaches the command line from zero: opening it, navigating (pwd, cd, ls), creating and moving files, running your code, installing software safely, PATH and 'command not found', piping, and reading real errors. You need nothing installed — the terminal is built into your computer." },
      { k: ["web track", "how the web works", "web and hosting", "web & hosting"], a: "The Web & Hosting track (20 lessons) closes the loop from code to internet: what a URL says, DNS, HTTP and status codes, then deploying for real with GitHub Pages and Netlify, custom domains, APIs and JSON, Node.js and npm, secrets, page performance, and a final 'publish a real site' project." },
      { k: ["http", "request", "response", "status code", "status codes", "200", "404", "500", "get", "post"], a: "HTTP is the conversation between browser and server: the browser sends a request (e.g. GET /css), the server answers with a status code plus content. 200 = OK, 301 = moved, 404 = not found, 403 = forbidden, 500 = the server broke. Every tab you open is this conversation happening. The Web track covers it in lessons 4-5, with a live status-code demo." },
      { k: ["dns", "domain", "url", "ip address", "custom domain", "myname.com", "com domain"], a: "A URL has a name (meenlearn.com) and a path (/css). DNS is the internet's phone book: it translates the name into an IP address, the machine's real number. A custom domain means buying a name and pointing it at your host with DNS records (A or CNAME). Web track lessons 2-3 explain the pieces, lesson 9 walks the domain setup." },
      { k: ["github pages", "deploy", "publish", "hosting", "host", "live site", "put online", "make my site public"], a: "GitHub Pages is free hosting for static sites: push your site to a repo, enable Pages in the repo's Settings (Source: main branch), and it goes live at yourname.github.com/repo. No cost, no credit card, and it runs the exact HTML/CSS/JS you've learned. It's the first deployment in the Web track — and this very site could run on it." },
      { k: ["netlify"], a: "Netlify is free hosting that's even lazier: drag your project folder onto its dashboard and it's live in seconds, or connect a GitHub repo for auto-deploys on every push. Great for experiments; GitHub Pages is the classic. Web track lesson 8 covers it step by step." },
      { k: ["node", "node.js", "npm", "backend", "server side"], a: "Node.js runs JavaScript outside the browser — on your machine or a server. It's how the language you learned in the JS track powers backends, tools and APIs. npm is its package manager (npm install express). Install from nodejs.org; the Web track has dedicated lessons 15-16." },
      { k: ["cors", "cross origin"], a: "CORS is the browser's rule that a page may only fetch data from servers that explicitly allow it. That's why a fetch that works in one place can fail when you open your local file — the server must send CORS headers for your origin. JS lesson 18 introduces fetch; Web lesson 14 covers CORS in depth." },
      { k: ["json"], a: "JSON is the web's data language: plain text shaped like objects and arrays — {\"name\":\"Ada\",\"age\":36}. APIs send it, JavaScript reads it with res.json(), Python with json.loads(). If you understand dicts/objects, you already understand JSON. Web track lesson 13." },
      { k: ["api", "apis", "endpoint", "rest"], a: "An API is a program talking to another program over HTTP: you request a URL (GET https://api.example.com/users) and it answers with data, usually JSON. Free public APIs are great practice — try the fetch lesson in the JavaScript track, then Web lesson 12 for the big picture." },
      { k: ["secret", "secrets", "env", "environment variable", "environment variables", "api key", "token", "leak", ".env"], a: "Secrets (API keys, tokens, passwords) belong in environment variables, never in code — committed code is history, and history is forever. The pattern: a .env file (added to .gitignore), loaded at runtime, and if a secret ever gets committed, ROTATE it immediately (delete it from history as a second step). Web lesson 17 is entirely about this." },
      { k: ["pip install", "pip", "package manager", "npm install"], a: "pip is Python's package manager: pip install <package> adds a library to your environment (ideally a venv — Python lesson 21). The equivalents: npm install in JavaScript-land, brew/apt/winget in your OS. All three answer the same question — 'how do I use other people's code?'" },
      { k: ["performance", "fast page", "slow page", "optimize", "load time", "speed up my site"], a: "Fast pages are small, few, and close: compress images (usually 80% of a page's weight), keep CSS/JS lean, load fonts with swap, and host somewhere reasonable. Web lesson 18 is the practical checklist — habits, not math." },

      /* ---------- programming ---------- */
      { k: ["algorithm", "step by step", "steps"], a: "An algorithm is a numbered list of steps that turns input into output — like a recipe for code. Write one down before coding: for a \"find the largest number\" task the steps are: start with the first number as the best, compare each next number, keep it if it's bigger, repeat until done. If you can write the steps in plain words, the code follows easily." },
      { k: ["variable", "store", "container", "variable is"], a: "A variable is a named box that holds a value: age = 21 means a box called age now holds 21. You can change it later (age = 22) and use the name anywhere. Good names — totalScore, not x — make code readable to your future self." },
      { k: ["data type", "types", "number", "string", "boolean", "type of data"], a: "Data comes in types: numbers (7, 3.5), text/strings (\"hello\"), and booleans (true/false). The type decides what you can do: you can add two numbers but \"5\" + \"3\" gives the text \"53\". Programs break often when types mix — this is one of the most common beginner bugs." },
      { k: ["input", "output", "processing", "IPO", "flow of data"], a: "Every program is Input → Processing → Output. A calculator takes input (2, 3, +), processes (adds them), outputs (5). A game takes input (your clicks), processes (moves the character), outputs (the new screen). When you're stuck, ask: what is the input? what processing happens? what should come out?" },
      { k: ["if", "else", "condition", "decision", "branch"], a: "If/else is how programs choose between paths: IF score >= 50 THEN print \"pass\" ELSE print \"retry\". The condition is a yes/no question; exactly one branch runs. You can chain several checks (if / else if / else) or combine questions with AND, OR and NOT." },
      { k: ["loop", "repeat", "while", "for loop"], a: "A loop repeats work: for each item in a list, do X. A while-loop repeats as long as a condition is true. Loops are how you process 1,000 rows instead of writing 1,000 lines. The classic bug: forgetting to change something inside the loop, so the condition never becomes false — an infinite loop." },
      { k: ["function", "def", "procedure", "method"], a: "A function is a named, reusable block of steps. You define it once (function add(a,b) { return a+b }) and call it anywhere with add(2,3). Functions take inputs (parameters) and usually return a result. They're the core tool for keeping code short and readable — if you copy-paste the same block twice, it wants to be a function." },
      { k: ["return", "parameter", "argument"], a: "Parameters are the inputs a function expects; the argument is the actual value you pass in. Return is how the function hands back a result to whoever called it — the code after return stops. A function that forgets return quietly gives back \"nothing\", which trips up many beginners." },
      { k: ["array", "list", "collection", "index"], a: "A list/array is an ordered collection of values: [\"apple\", \"banana\"]. Items are reached by position (index), and in most languages the first item is index 0 — that's why \"arrays start at zero\" is such a famous fact. Lists let you store any number of values in one variable and loop over them." },
      { k: ["dictionary", "map", "object", "key value", "dict"], a: "A dictionary (or map/object) stores values under named keys: { \"name\": \"Ada\", \"age\": 36 }. Unlike lists, you look things up by label, not position. Real programs are built mostly from these: a user is a dict, a database row is a dict, a JSON file is a family of dicts." },
      { k: ["error", "bug", "debug", "debugging", "crash"], a: "Debugging is the loop: run → see the error → guess the cause → fix → run again. Read the error message bottom-up: it usually tells you the line and the reason. Add print statements to show what your variables actually hold at each step — seeing the real values beats guessing. If code changes, re-run; never debug from memory." },
      { k: ["pseudocode", "pseudo code"], a: "Pseudocode is the algorithm written in loose, plain-English steps, ignoring exact syntax: 1. ask for a number 2. if it is negative, ask again 3. double it 4. show the result. Writing it first forces you to think through the logic before syntax gets in the way — the fastest habit for not getting stuck." },
      { k: ["flowchart", "diagram"], a: "A flowchart is the same steps drawn as boxes (processes), diamonds (decisions) and arrows (direction). It's great for spotting problems early: if you can't draw the arrow path, you don't fully understand the logic. Every decision diamond has two exits — yes and no — and both must lead somewhere." },
      { k: ["recursion", "recursive", "call itself"], a: "Recursion is a function that calls itself with a smaller problem, until it hits a base case it can answer directly. Example: to count down from 3: say 3, then ask yourself to count down from 2. Two rules: always have a base case, and always shrink the problem. Get those wrong and it runs forever (or crashes)." },
      { k: ["trace", "trace table", "walk through", "dry run"], a: "A trace table is where you run the code on paper: one row per loop pass, one column per variable, writing its value after each line. For loops and counters, tracing by hand catches off-by-one bugs before you even open the editor. It feels slow at first, then it becomes your superpower." },
      { k: ["test", "unit test", "testing", "assert"], a: "Testing means checking your code against expectations: given input X, output must be Y. Start by hand (trace tables), then write small automated checks: if add(2,3) is not 5, fail. Testing a few edge cases — empty lists, zero, negative numbers — is where most bugs hide." },
      { k: ["complexity", "fast", "slow", "efficiency", "big o"], a: "Not all solutions are equally fast. Checking one list item is constant; scanning a list of n items is linear; scanning every pair of items is quadratic — 1,000 items is fine, 10,000 items is slow. You don't need the math yet; just develop the habit of asking \"how many times does this inner loop run?\"" },
      { k: ["program", "what is a program"], a: "A program is a precise list of instructions a computer follows exactly as written. Computers are fast but literal: no guessing, no intuition. That's why learning to program is mostly learning to be precise with steps, data and decisions — the Programming track covers exactly that." },
      { k: ["programming", "programming track"], a: "The Programming track (20 lessons) covers: what programs are, algorithms, data and types, variables, decisions, loops, functions, lists, dictionaries, errors, pseudocode, flowcharts, recursion, trace tables, testing, complexity, and a final build-together project. It's the recommended first track — no prerequisites." },

      /* ---------- HTML ---------- */
      { k: ["doctype", "<!doctype html>"], a: "<!doctype html> is line one of every page. It tells the browser \"use modern standards mode\" — without it, browsers fall back to an old compatibility mode and layouts can break subtly. It's not a tag (no closing part), it's a declaration, and it must be the very first thing in the file." },
      { k: ["head", "<head>", "head tag", "what goes in head"], a: "The <head> is the page's behind-the-scenes section — the browser reads it but does NOT display it on screen. It holds: the <title> (shown in the tab and search results), <meta> tags (charset, viewport for mobile, description for search engines), links to stylesheets and fonts, and the page's icons. Everything people can SEE goes in <body> instead." },
      { k: ["body", "<body>"], a: "The <body> contains everything the user actually sees and interacts with: text, images, buttons, videos. A page has exactly one <body>. The rule that fixes 90% of beginner confusion: visible content in <body>, invisible metadata in <head>." },
      { k: ["skeleton", "document structure", "basic structure", "empty page", "template"], a: "The minimum skeleton of any HTML page: <!doctype html>, <html lang=\"en\">, a <head> with <meta charset> + <title>, and a <body>. The lang attribute helps screen readers and translation. Every page on MeenLearn builds from this skeleton — lesson 2 of the HTML track." },
      { k: ["tag", "element", "opening tag", "closing tag"], a: "An element is the basic building block: an opening tag, content, and a closing tag — <p>Hello</p>. Some elements have no content and need no closing tag, like <img>. The tag name is the element name; text inside tags is content; the small name=\"value\" parts on the tag are attributes." },
      { k: ["attribute", "attributes"], a: "Attributes add extra information to an element: <a href=\"https://example.com\"> link. They come in name=\"value\" pairs, values always in quotes. Common ones: href (link target), src (file source), alt (image description), class (styling hook), id (unique identifier)." },
      { k: ["heading", "h1", "h2", "heading structure"], a: "Headings are <h1> to <h6>, h1 biggest. Each page should have exactly one <h1> — the page's main title — and use h2, h3 in order for sections and subsections. Don't pick heading levels by size; that's what CSS is for. The order also tells screen readers and Google how your page is organized." },
      { k: ["link", "anchor", "<a>", "hyperlink"], a: "Links are <a href=\"...\">text</a>. The href points to the destination: another page (about.html), a section of the same page (#contact), or a full URL. You can link two pages together, or a page to its own section using id + #. Links open in a new tab with target=\"_blank\" (add rel=\"noopener\" for security)." },
      { k: ["image", "<img>", "picture", "photo"], a: "Images: <img src=\"cat.jpg\" alt=\"A sleeping cat\">. src is the file location, alt is the text shown if the image fails and read by screen readers — always write meaningful alt text. Images are inline elements, so wrap them or use CSS to control their size. For modern pages, serve small files (JPEG/WEBP for photos, SVG for icons)." },
      { k: ["list", "<ul>", "<ol>", "<li>", "unordered list", "ordered list"], a: "Lists: <ul> for bullet lists, <ol> for numbered lists, and <li> for each item. You can nest lists inside <li> for sub-items. For a checklist-style list of steps without numbers, use <ul>; if the order matters (recipes, rankings), use <ol>." },
      { k: ["table", "<table>", "rows", "columns", "tabular data"], a: "Tables are for real tabular data (not for page layout): <table> contains <tr> rows, each row has <th> header cells and <td> data cells. Wrap the first row group in <thead>, data in <tbody>. A common beginner mistake: using tables to build page layouts — that's what CSS grid/flexbox are for." },
      { k: ["form", "<form>", "input", "submit"], a: "Forms collect user input: a <form> wraps <input> fields, and a submit button sends them. Each input has a type (text, email, password, number, checkbox, date...) and an <label> tied to it for accessibility. The form's action tells the server where the data goes. Learn forms in HTML lessons 8-9, then make them work with JavaScript." },
      { k: ["button", "<button>", "label"], a: "Buttons come in two kinds: <button> (plain, you wire it up with JavaScript) and <input type=\"submit\"> (works inside forms automatically). Always pair inputs with <label for=\"...\"> pointing at the input's id — it makes the clickable area bigger and is essential for screen readers." },
      { k: ["semantic", "semantic html", "main", "nav", "section", "article", "aside", "footer", "header", "landmark"], a: "Semantic tags describe what a section IS: <header> (top of page), <nav> (links), <main> (the one main area), <article> (self-contained content), <section> (themed group), <aside> (side info), <footer> (bottom). Instead of <div class=\"nav\">, just use <nav>. Screen readers, search engines and other developers all thank you." },
      { k: ["block", "inline", "block vs inline", "display type"], a: "Block elements (div, p, h1, ul) start on a new line and stretch full width. Inline elements (span, a, strong, img) flow within text. This default behavior comes from the element's type — but CSS's display property can override it. Knowing the default makes debugging layout far easier." },
      { k: ["entity", "special character", "&lt;", "&amp;", "escape"], a: "HTML has a few reserved symbols, so special characters use entities: &lt; for <, &gt; for >, &amp; for &, &quot; for \". You see this when you want to display code: &lt;p&gt;Hello&lt;/p&gt; shows the text <p>Hello</p> instead of creating a real element. Space is &nbsp; (non-breaking)." },
      { k: ["video", "audio", "media"], a: "Media is built in: <video src=\"clip.mp4\" controls></video> and <audio src=\"song.mp3\" controls></audio>. The controls attribute adds play/pause UI. For videos with a poster or multiple formats, add a <source> element inside. For YouTube, an <iframe> embed is the standard approach." },
      { k: ["iframe", "embed", "frame", "youtube"], a: "An <iframe> embeds another page inside your page: <iframe src=\"https://www.youtube.com/embed/...\" title=\"Video\"></iframe>. Use it for maps, videos and third-party widgets. Always give it a title (accessibility) and set width/height. Note: sandboxing controls what embedded scripts can do." },
      { k: ["meta", "meta tag", "seo", "open graph", "viewport", "description meta"], a: "Meta tags sit in <head>. The essentials: <meta charset=\"UTF-8\"> (text encoding — must be near the top), <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\"> (mobile support), <meta name=\"description\"> (the line search engines show). Open Graph tags (og:title, og:image) control how links look when shared on social media." },
      { k: ["accessibility", "a11y", "aria", "screen reader"], a: "Accessibility basics: meaningful alt text on images, labels on every input, one h1 per page, semantic tags, sufficient color contrast, and keyboard-reachable links. Test by tabbing through your page with the keyboard — if you can't use it that way, a screen-reader user can't either." },
      { k: ["validate", "validation", "check my html", "w3c", "errors in html"], a: "Two ways to check HTML: the W3C validator (validator.w3.org) reports exact errors, and your browser's DevTools (right-click → Inspect) shows the structure as parsed. Fix errors in the order the validator reports them — unclosed tags, missing attributes, and duplicate ids are the usual suspects." },
      { k: ["html", "html track", "learn html"], a: "The HTML track has 20 lessons: the full skeleton, head vs body in depth, text, links, images, lists, tables, forms, semantic layout, attributes, block vs inline, special characters, media, iframes, SEO meta, accessibility, validation, and a final project: a complete personal page." },
      { k: ["nesting", "parent", "child", "inside"], a: "Nesting is putting elements inside elements: <div><p>text</p></div>. The outside element is the parent; the inside is the child. Nest logically (a list item contains a link, a section contains headings), and always close tags in the reverse order you opened them — mismatched nesting is the #1 beginner source of weird layout bugs." },

      /* ---------- CSS ---------- */
      { k: ["css", "stylesheet", "what is css", "learn css"], a: "CSS (Cascading Style Sheets) controls how HTML looks: colors, spacing, layout, fonts and motion. You write rules — a selector plus a set of properties: p { color: teal; margin-bottom: 12px }. Three ways to add it: a separate .css file (the right way), a <style> block in <head>, or style=\"\" attributes (avoid except for quick tests). The CSS track has 20 lessons from colors to grid to animation." },
      { k: ["selector", "selectors", "target"], a: "A selector picks which elements a rule applies to: p (all paragraphs), .card (all elements with class=\"card\"), #logo (the one element with id=\"logo\"). You can combine them: nav a (links inside nav), .card:hover (a card being hovered). Specificity decides which rule wins when two conflict — id beats class beats element." },
      { k: ["color", "colors", "hex", "rgb", "transparent"], a: "Colors can be named (teal), hex (#F5B23D), rgb/rgba (rgb(245,178,61,0.5) — the last number is opacity), or hsl. Modern CSS also has color-mix() to blend two colors. Practical tip: define your palette once at the top as CSS variables and reuse them, so a rebrand is a one-line change." },
      { k: ["font", "typography", "text", "line-height", "font-family"], a: "Text properties: font-family (always end with a fallback like sans-serif), font-size (use rem for text), font-weight (400 normal, 700 bold), line-height (1.5–1.7 for readable body text), letter-spacing, text-align. For web fonts, load them in <head> with <link> and set font-display: swap so text shows while the font downloads." },
      { k: ["box model", "padding", "border", "margin box", "content box"], a: "Every element is a box with four layers: content → padding (space inside) → border → margin (space outside). Total width = content + padding + border + margin. Set box-sizing: border-box globally (html * { box-sizing: border-box }) so width includes padding and border — this makes layout math sane. This is the single most important CSS concept; it's lesson 5 of the CSS track." },
      { k: ["margin", "margin collapse", "spac"], a: "Margin is the space outside a box. Vertical margins between block siblings collapse: if one has 20px bottom and the next has 30px top, the gap is 30px, not 50px — this surprises everyone. Modern fix: prefer flexbox/grid with gap instead of margins for spacing, and use margin-top on the parent rather than margin-bottom on the last child." },
      { k: ["display", "display block", "display none", "display inline"], a: "display changes how an element participates in layout: block (new line, full width), inline (flows in text), inline-block (inline flow but can have width/height), none (removed entirely), flex and grid (the two real layout systems). display:none hides an element AND removes its space; visibility:hidden keeps the space. Toggling display:none is the classic way to show/hide things with JavaScript." },
      { k: ["position", "absolute", "relative", "fixed", "sticky", "positioning"], a: "position controls layout behavior: static (default, in normal flow), relative (offset from its own spot — the anchor for absolute children), absolute (removed from flow, placed relative to nearest positioned ancestor), fixed (stays in place while the page scrolls), sticky (in flow until a scroll threshold, then sticks). The classic bug: absolute has no effect because no ancestor is relative — the #1 \"my element is in the wrong place\" cause." },
      { k: ["z-index", "stacking", "overlap", "on top"], a: "z-index decides which element paints on top when positioned elements overlap: higher number = front. It only works on positioned elements (position != static) or flex/grid items. Start small (1, 2, 10, 100 for modals) instead of 9999 — you'll hit its limits. If two elements overlap but z-index seems ignored, check that both are actually positioned." },
      { k: ["flexbox", "flex", "flex container", "flex-item"], a: "Flexbox lays out items in one direction (a row or column). Set display: flex on the parent, then control it with: flex-direction (row/column), justify-content (main-axis alignment), align-items (cross-axis), gap (spacing), and flex-wrap. On items: flex: 1 to grow, order to rearrange. It's the default choice for toolbars, card rows, centering, and navigation. Lessons 10-11." },
      { k: ["grid", "css grid", "grid-template", "grid layout"], a: "CSS Grid lays out in two dimensions (rows AND columns). Set display: grid on the parent, grid-template-columns: repeat(3, 1fr) for three equal columns, gap for spacing, and place children — they auto-flow into cells. Span items across cells with grid-column: span 2. Use grid for page-level layouts (header/sidebar/main), flexbox for one-dimensional rows. Lessons 12-13." },
      { k: ["responsive", "media query", "mobile", "mobile first", "breakpoint"], a: "Responsive design means the page adapts to screen size. You write base styles for small screens, then @media (min-width: 768px) { ... } to add layout as space appears (mobile-first). Breakpoints are where your layout actually breaks — common stops: 640, 768, 1024px. Also make images fluid (max-width: 100%) and avoid fixed pixel widths. Lesson 14." },
      { k: ["rem", "em", "px", "units", "vh", "vw", "clamp", "percentage"], a: "Units: px is fixed; rem is relative to the root font size (1rem = 16px by default — good for text); em is relative to the element's own font size (good for scaling); % is relative to the parent; vw/vh are viewport-based (100vh = full screen height); fr is grid-only (fraction of space). Modern trick: clamp(1rem, 2.5vw, 1.5rem) scales text fluidly between a min and max. Rule of thumb: rem for text, %/fr for layout." },
      { k: ["transition", "hover effect", "smooth", "animate on hover"], a: "transitions animate a property change over time: .card { transition: transform .2s ease; } .card:hover { transform: translateY(-4px); }. You must list the properties you want to animate (transition: all is convenient but slower and can animate things you didn't intend). Duration 0.15–0.3s feels snappy; ease or ease-out curves feel natural. Transitions are how hover effects stop feeling robotic. Lesson 16." },
      { k: ["animation", "keyframes", "@keyframes", "looping", "loop animation"], a: "For full control, @keyframes defines start/end (or any % stops) and animation: name duration timing infinite applies it: @keyframes float { 50% { transform: translateY(-10px); } } .ball { animation: float 3s ease-in-out infinite; }. Use keyframes for repeating motion (floating shapes, pulsing, spinners); use transitions for reacting to user actions like hover. Lesson 17." },
      { k: ["pseudo", ":hover", "::before", "::after", "pseudo-class", "pseudo-element"], a: "Two families: pseudo-CLASSES (single colon) target states — :hover, :focus, :first-child, :nth-child(2n) — and pseudo-ELEMENTS (double colon) target virtual parts — ::before and ::after add generated content (great for shapes, underlines, icons without extra HTML), plus ::selection and ::placeholder." },
      { k: ["variable", "css variable", "custom property", ":root"], a: "CSS variables (custom properties) let you store values and reuse them: :root { --gold: #F5B23D; } .btn { background: var(--gold); }. Define them at the top (or in :root), reference with var(). Change one value, the whole theme updates. MeenLearn's entire dark/light theme works this way — every color is a variable. Lesson 19." },
      { k: ["specificity", "override", "why my css doesn", "cascade", "!important"], a: "When two rules conflict, the winner is decided by specificity (id > class > element), then source order (later wins). That's why your rule \"doesn't work\": a more specific selector or a later rule is overriding it. Check in DevTools which rule wins (crossed-out properties show overrides). Avoid !important unless truly necessary — it breaks the natural system and becomes unmaintainable." },
      { k: ["cascade", "cascading"], a: "The \"C\" in CSS: when multiple rules target the same element, they don't just stack — they cascade. The browser decides the winner by: origin/!important, then specificity (id beats class beats element), then source order (later wins). Understanding this one mechanism explains almost every \"why doesn't my CSS work?\" question." },
      { k: ["layout", "layout systems", "flexbox vs grid"], a: "Rule of thumb: flexbox for one dimension (a toolbar, a row of buttons, centering one thing), grid for two dimensions (page layout, card galleries). Classic modern page: grid for the overall skeleton (header/sidebar/main/footer), flexbox inside components (nav links, button groups). Everything else — floats, absolute positioning for layout — is legacy." },
      { k: ["border-radius", "radius", "rounded", "shadow", "box-shadow", "background"], a: "Visual polish in one line each: border-radius: 12px rounds corners (50% makes circles), box-shadow: 0 10px 30px rgba(0,0,0,.2) adds depth, background: linear-gradient(135deg, #111, #333) blends colors. Small radii (4-8px) feel precise; large (16-24px) feel friendly. Shadows with a low opacity and large blur read as \"floating\" — the modern default." },
      { k: ["css track"], a: "The CSS track has 20 lessons: connecting CSS to HTML, selectors, colors and units, typography, the box model, margins, display, position, z-index, flexbox (2), grid (2), responsive design, responsive units, transitions, animations, pseudo-classes, CSS variables, and a final project: a fully responsive card layout." },

      /* ---------- JavaScript ---------- */
      { k: ["javascript", "js", "what is javascript", "learn js"], a: "JavaScript is the programming language of the web — it runs in every browser and (via Node.js) on servers too. HTML is structure, CSS is style, JavaScript is behavior: it reacts to clicks, updates the page without reloading, and talks to servers. The track has 20 lessons: language basics, then the DOM, events, storage, and a full to-do app." },
      { k: ["let", "const", "var", "which variable"], a: "Use const by default — the variable can't be reassigned, which prevents whole bug classes. Use let when the value must change (a counter). Avoid var — it's the old way, with confusing scoping rules. One-line rule: const unless you have a reason for let, never var." },
      { k: ["typeof", "type of", "number", "string", "boolean", "data types js"], a: "JavaScript types: string (\"hi\"), number (42 — integers and decimals are the same type), boolean (true/false), undefined (declared but no value), null (intentionally empty), object (including arrays), symbol, bigint. typeof tells you which, with two famous quirks: typeof null is \"object\" (a historic bug) and typeof an undeclared variable throws — always check with typeof x === \"undefined\" first." },
      { k: ["operator", "comparison", "===", "==", "difference between =="], a: "Use === (strict equality) and !== — they compare value AND type. The loose == does weird type conversion: \"5\" == 5 is true, but \"5\" === 5 is false. Math: + - * / % (modulo = remainder). The += shortcut (count += 1) means count = count + 1. A classic bug: if (count = 0) — one = is assignment, not comparison; it sets count to 0 AND the condition is always false." },
      { k: ["conditional", "if statement", "ternary", "? :"], a: "if (condition) { ... } else if (other) { ... } else { ... } — exactly one branch runs. The condition must be a boolean expression: x > 10, name === \"Ada\". The ternary is a one-line if for choosing values: const label = score >= 50 ? \"pass\" : \"retry\". Use it for values, not for logic blocks." },
      { k: ["loop js", "for loop js", "while js"], a: "Three common forms: for (let i = 0; i < items.length; i++) — index loop; for (const item of items) — cleanest for going through values; while (condition) — repeat until something changes. For arrays, the for...of form plus .map/.filter/.forEach usually beats manual index math. Always change something inside a while loop or it never ends." },
      { k: ["function js", "arrow function", "=>", "callback"], a: "Define with function add(a, b) { return a + b } or the shorter arrow form: const add = (a, b) => a + b (one-line bodies omit braces and return). A callback is a function you pass to another function, which calls it later — setTimeout(fn, 1000), button.addEventListener(\"click\", fn), array.map(fn). Callbacks are how JavaScript coordinates: \"do X, then call me.\""},
      { k: ["array", "map", "filter", "array methods", "push"], a: "Array methods are the workhorses: push() adds, .length is the count, .includes(x) checks, .map() transforms every item (returns new array), .filter() keeps items that pass a test, .find() gets the first match, .reduce() folds into one value. Chaining is normal: users.filter(u => u.active).map(u => u.name). map/filter don't change the original — they return new arrays." },
      { k: ["object", "object js", "dot notation", "bracket notation"], a: "Objects group related data: const user = { name: \"Ada\", age: 36, greet() { return \"Hi\"; } }. Read with dot (user.name) or brackets (user[\"name\"]) — brackets let you build the key dynamically (user[lang]). Objects are passed by reference: a = user doesn't copy, it points to the same object — a famous source of \"why did my data change?\" bugs." },
      { k: ["dom", "document", "document.getelementbyid", "select element", "queryselector"], a: "The DOM is the page as a live tree of objects JavaScript can read and change. Find elements: document.querySelector(\".card\") (first match, any CSS selector) or querySelectorAll (all matches). Then change them: el.textContent = \"new\", el.classList.add(\"active\"), el.style.color = \"red\", el.setAttribute(...). querySelector is the modern default; getElementById is fine too." },
      { k: ["event", "click event", "add event listener", "addEventListener", "listener"], a: "Events are how the page tells your code something happened: click, input, submit, keydown, scroll... Attach a handler: button.addEventListener(\"click\", () => { ... }). The handler receives an event object with details (event.target, event.key, event.preventDefault()). The classic bug: forgetting () => after the function name — addEventListener(btn, handler) vs addEventListener(btn, handler()) — the () actually calls the function immediately." },
      { k: ["form js", "form submit", "preventdefault", "handle form"], a: "To handle a form in JavaScript: form.addEventListener(\"submit\", (event) => { event.preventDefault(); /* stop the page reload */ const data = new FormData(form); ... }). Reading values: input.value, or FormData(form).get(\"name\"). Without preventDefault, the browser submits and reloads the page, killing your JS. Lesson 13 of the JavaScript track." },
      { k: ["settimeout", "setinterval", "timer", "delay", "after"], a: "setTimeout(fn, ms) runs fn once after the delay; setInterval(fn, ms) repeats it forever until you clearInterval(id). Both return an id you need to cancel. Common uses: close a toast after 3s, poll a status every 5s, type-out animations. Remember: JavaScript is single-threaded — the delay doesn't pause your code, it just schedules the callback later." },
      { k: ["localstorage", "save", "persist", "remember", "storage"], a: "localStorage keeps simple strings in the browser forever (until cleared): localStorage.setItem(\"score\", 42) to save, getItem to read, removeItem to delete. Values must be strings, so objects need JSON.stringify() to save and JSON.parse() to read: localStorage.setItem(\"todos\", JSON.stringify(todos)). Perfect for settings, progress and to-do lists — this is how MeenLearn saves your lesson progress. No server, no account." },
      { k: ["try", "catch", "throw", "error handling js"], a: "try { risky code } catch (error) { handle it } finally { always runs }. Use it around things that can fail: JSON.parse of user input, network requests, file reads. Instead of your app crashing with a mystery, you can show a friendly message or a fallback value. throw lets you raise your own errors. Lesson 17." },
      { k: ["fetch", "api", "get data", "request", "cors", "server"], a: "fetch(url) asks a server for data and returns a Promise: fetch(\"https://api.example.com/users\").then(res => res.json()).then(users => { ... }) or the cleaner async form: const res = await fetch(url); const data = await res.json(); (inside an async function). Two gotchas for beginners: fetch only rejects on network failure — check res.ok for HTTP errors — and the server must allow your site (CORS). Lesson 18." },
      { k: ["async", "await", "promise", "async/await"], a: "A Promise is a future value: fetch, setTimeout-as-promise, etc. await pauses an async function until the promise settles, making async code read top-to-bottom: const data = await fetch(url).then(d => d.json()) becomes const res = await fetch(url); const data = await res.json();. Rules: await only works inside async functions, and it never blocks the page — other code keeps running while it waits." },
      { k: ["bug", "debug js", "console", "console.log", "error"], a: "Your debugging toolbox: console.log(x) prints values to DevTools (F12 → Console), console.table(list) pretty-prints arrays/objects, console.error for problems, and the Sources tab has breakpoints. Read errors from the bottom: the message + file + line. The most common JS bugs: typos in names, calling a function instead of passing it (handler() vs handler), and assuming data has a shape it doesn't (always check with console.log first)." },
      { k: ["javascript track"], a: "The JavaScript track has 21 lessons: what JS is, variables (let/const), types, operators, conditionals, loops, functions and arrows, arrays and methods, objects, the DOM, changing the page, events, forms, timers, localStorage, mini data flows, try/catch, fetch, modular thinking, and a final project: a working to-do app." },

      /* ---------- Python ---------- */
      { k: ["python", "learn python", "what is python", "python track"], a: "Python is a general-purpose language famous for clean, readable syntax — the indentation IS the code structure. It's used for web backends, data science, automation, AI, and scripts. The track has 20 lessons: setup, basics, logic, data, files, modules, errors, and two mini-projects (a guessing game and a contact book)." },
      { k: ["install python", "setup", "where to run", "idletutorial", "repl", "how to run python"], a: "You don't need to install anything to start: use an online editor like Python Tutors or a browser IDE. For a real setup: download from python.org (check \"Add to PATH\" on Windows), then in your terminal type python --version to verify, and run a file with python myscript.py. To explore interactively, type just python and you get the REPL — type an expression, press Enter, see the result." },
      { k: ["print", "print function", "output python"], a: "print() shows output: print(\"Hello\") or print(\"Score:\", score, sep=\" \"). Multiple arguments are joined by spaces by default; sep= and end= customize that. print is also your #1 debugging tool: sprinkle it around to see what values actually are at each step." },
      { k: ["input", "input function", "user input", "ask"], a: "input() pauses and reads one line from the user — but ALWAYS returns a string, even for numbers: age = int(input(\"Age: \")). Forgetting int() is the classic beginner crash (\"'str' + 'int'\" errors). Store it as a string only if you truly want text; convert immediately if it's a number." },
      { k: ["string python", "string methods", "f-string", "string formatting", "concatenate"], a: "Strings: s.upper(), s.lower(), s.strip(), s.replace(\"a\",\"b\"), s.split(\",\"), s.find(\"x\"), len(s), \"a\" in s. F-strings are the modern way to build text: f\"Hello, {name}, you have {count} items\" — clean and readable. Avoid building text with + when you have many pieces; f-strings handle it in one line." },
      { k: ["python types", "int", "float", "type()"], a: "Core types: int (42), float (3.5), str (\"hi\"), bool (True/False — capital letters in Python!), list, dict, tuple, set, None (the empty value). type(x) tells you. Conversions: int(\"42\"), float(\"3.5\"), str(7), bool(0) is False (0, \"\", empty list are \"falsy\"). Python 3 division / always gives a float; use // for integer division and % for remainder." },
      { k: ["if python", "elif", "condition python"], a: "Python's if uses colons and indentation (no braces): if score >= 50:\\n    print(\"pass\")\\nelif score >= 30:\\n    print(\"retry\")\\nelse:\\n    print(\"fail\"). The indented block IS the body — inconsistent indentation is an error, which is actually a feature: it forces readable code. Conditions combine with and, or, not: if 0 <= score <= 100 (that's a valid range check)." },
      { k: ["loop python", "for python", "range", "while python"], a: "Two loops: for item in list — go through items; for i in range(n) — 0 to n-1 (range(5) → 0,1,2,3,4); while condition — repeat until it's false. range also takes start/stop/step: range(2, 10, 2) → 2,4,6,8. The classic bug: a while loop that never changes the condition — infinite loop (Ctrl+C out of it)." },
      { k: ["function python", "def", "def function", "return python"], a: "Define with def: def greet(name):\\n    return f\"Hello, {name}\". Call it: greet(\"Ada\"). Parameters can have defaults: def greet(name, greeting=\"Hi\"). Return gives a result back — a function with no return gives None. Python functions can be assigned to variables and passed around like any value (that's the foundation of callbacks and map)."},
      { k: ["list python", "list methods", "append", "index python"], a: "Lists: fruits = [\"apple\", \"banana\"]; fruits.append(\"cherry\"); fruits[0] (first), fruits[-1] (last!), len(fruits). Useful: .remove(x), .index(x), .sort(), .copy() (shallow). Slicing: nums[1:4] is items at 1,2,3; nums[:3] is the first three; nums[::2] every second. Lists are mutable — append changes the original." },
      { k: ["dictionary python", "dict", "dict methods", "keys"], a: "Dicts: user = {\"name\": \"Ada\", \"age\": 36}; user[\"name\"] to read, user[\"age\"] = 37 to update, \"email\" in user to check, .get(\"email\", \"none\") to read with a default (no crash if missing), .keys(), .values(), .items() to loop. Looping: for key, value in user.items():. Dicts are the workhorse of real Python code — configs, API responses, CSV rows all become dicts." },
      { k: ["tuple", "set", "tuple python", "set python"], a: "A tuple is an immutable list — (1, 2, 3) — used for fixed collections and as dict keys (lists can't be keys). Unpacking: x, y = point. A set is an unordered collection with no duplicates: {1, 2, 2, 3} → {1, 2}. Sets shine for membership tests (\"x\" in my_set is very fast), removing duplicates (set(my_list)), and math (intersection, union)." },
      { k: ["list comprehension", "comprehension", "one line list"], a: "A list comprehension builds a list in one readable line: [x * 2 for x in numbers] — \"double each x\". Add a filter: [x for x in nums if x > 10]. They replace 3-line for+append patterns and are the Pythonic idiom. Keep them to one or two parts max — if it's hard to read, use a normal loop." },
      { k: ["error python", "exception", "try except", "traceback"], a: "Python errors show a traceback: read it from the bottom — the last line is the actual error, the lines above show where it happened. try: / except ValueError: / except Exception as e: wraps risky code. Use specific exceptions (ValueError, KeyError, FileNotFoundError) instead of bare except. The classic beginner ones: IndexError (list out of range), KeyError (dict missing key), TypeError (wrong type for the operation), IndentationError (misaligned code)." },
      { k: ["file python", "read file", "write file", "open()", "with open"], a: "The with-open pattern (it closes the file automatically): with open(\"data.txt\") as f:\\n    lines = f.read(). Write: with open(\"out.txt\", \"w\") as f:\\n    f.write(\"hello\"). Modes: \"r\" read, \"w\" write (wipes the file!), \"a\" append. For CSV: import csv; csv.reader / csv.writer handle commas, quotes and newlines for you. Lesson 19." },
      { k: ["import", "module", "pip", "package", "install package"], a: "Modules let you use other people's code: import math; math.sqrt(16). from os import path gives you a specific name. Standard library (math, os, json, csv, datetime) is built in. For extras, pip install the package: pip install requests, then import requests. pip is the package manager, PyPI is the library of packages. Lesson 20." },
      { k: ["venv", "virtual environment", "virtualenv", "environment"], a: "A virtual environment is a sandboxed Python for one project, so its installed packages don't clash with other projects: python -m venv .venv, then activate it (Windows: .venv\\Scripts\\activate, macOS/Linux: source .venv/bin/activate). Install inside it with pip. Deactivate with deactivate. Add .venv to .gitignore. Lesson 21." },
      { k: ["pandas", "data analysis", "dataframe", "csv analysis"], a: "For real data work, pandas is the standard: import pandas as pd; df = pd.read_csv(\"sales.csv\"); df.head(), df.describe(), df[df[\"amount\"] > 100]. A DataFrame is a spreadsheet in code. You don't need to master it now — the Python track's data lesson shows the pattern (load → filter → summarize → export), and pandas is the next step after you're comfortable with lists and dicts." },
      { k: ["python project", "guessing game", "contact book", "mini project"], a: "The track ends with two projects you build yourself: a number-guessing game (loops + conditionals + input + random) and a contact book (dicts + file saving + a command menu). Both use only what the track taught you — that's the point: by lesson 26 you've combined variables, logic, data, functions and files into real programs. Try them before reading the solution." },
      { k: ["python vs javascript", "difference python js", "which is better"], a: "They're both general-purpose, but different by default: Python runs mainly on your machine/server with explicit types and indentation-as-structure; JavaScript runs in every browser, is dynamic, and is the language of the web. Learn JavaScript for web interactivity, Python for scripts, data and backends. The concepts (variables, loops, functions, data) transfer 1:1 — syntax is the only real difference." },

      /* ---------- SQL ---------- */
      { k: ["sql", "database", "what is sql", "learn sql", "sql track"], a: "SQL (Structured Query Language) is how you talk to relational databases — the tables behind almost every app you use. You ask questions (SELECT), filter (WHERE), combine tables (JOIN), and modify data (INSERT/UPDATE/DELETE). The track has 20 lessons from your first SELECT to joins, keys, indexes and a shop-analysis project. No install needed — practice in any online SQL sandbox." },
      { k: ["table", "row", "column", "record", "relational", "schema"], a: "A table is like a spreadsheet: rows are records (one customer), columns are fields (name, email, joined_on). The columns' names and types are the schema. In relational design, every row should be one thing and one fact, and related tables link by id. The single most important rule: design so each column holds one atomic value (not \"John Smith, age 30\" in one column)." },
      { k: ["select", "select *", "query"], a: "SELECT is the workhorse: SELECT name, email FROM customers; asks for those columns from every row. SELECT * returns all columns — fine for exploring, avoid in production (it breaks when columns change). Always end with a semicolon. You can compute in SELECT: SELECT price * quantity AS total FROM orders;" },
      { k: ["where", "filter", "condition sql", "comparison"], a: "WHERE filters rows before any other clause: SELECT name FROM customers WHERE country = 'Nigeria';. Operators: =, <>, >, <, >=, <=, plus LIKE for patterns (name LIKE 'A%'), IN for lists (country IN ('NG','GH')), BETWEEN for ranges, and IS NULL for empty values (never = NULL — that's the #1 SQL trap, lesson 19)." },
      { k: ["and", "or", "not", "logic sql", "combine conditions"], a: "Combine conditions: WHERE country = 'NG' AND age > 18, or WHERE age < 18 OR age > 65. NOT inverts. Precedence: NOT > AND > OR — so use parentheses when mixing AND and OR to be explicit. When in doubt, add the parentheses: WHERE (a OR b) AND c." },
      { k: ["order by", "sort", "asc", "desc"], a: "ORDER BY sorts results: SELECT name, age FROM users ORDER BY age DESC; (newest first). Default is ASC (ascending). Sort by multiple columns: ORDER BY country ASC, name ASC — country first, then name within each country. Put ORDER BY at the end of the query (after LIMIT in practice, though it's evaluated before it)." },
      { k: ["limit", "offset", "pagination", "first 10"], a: "LIMIT caps the rows: SELECT * FROM products LIMIT 10; gives the first 10. OFFSET skips rows before limiting — the basis of pagination: page 2 is LIMIT 10 OFFSET 10. Note: MySQL uses LIMIT n OFFSET m; SQLite and PostgreSQL also accept LIMIT m OFFSET n (or LIMIT m, m). Always combine with ORDER BY — otherwise \"the first 10\" is undefined." },
      { k: ["count", "sum", "avg", "min", "max", "aggregate", "aggregation"], a: "Aggregates collapse many rows into one value: COUNT(*) counts rows, SUM(price) totals, AVG(age) averages, MIN/MAX find extremes. They ignore NULLs. COUNT(column) counts non-NULL values in that column specifically. Use them with SELECT to get summary stats: SELECT COUNT(*) FROM customers; → one number. Lesson 8." },
      { k: ["group by", "grouping", "having"], a: "GROUP BY turns many rows into one row per group: SELECT country, COUNT(*) FROM customers GROUP BY country; gives a count per country. Any column in SELECT that isn't aggregated must appear in GROUP BY. HAVING filters groups (WHERE filters rows): ... GROUP BY country HAVING COUNT(*) > 10; keeps only countries with more than 10 customers. Order of operations: WHERE → GROUP BY → HAVING → ORDER BY → LIMIT." },
      { k: ["join", "joins", "combining tables", "join tables"], a: "JOINs combine rows from two (or more) tables that share a value: SELECT c.name, o.total FROM customers c JOIN orders o ON c.id = o.customer_id; — for each customer, attach their orders. The ON clause says how they match. JOINs are how relational databases are powerful: keep tables normalized (one fact per row) and reassemble views with joins. Lessons 10-11." },
      { k: ["inner join", "left join", "join types", "difference joins"], a: "INNER JOIN: only rows that match in BOTH tables (a customer with no orders disappears). LEFT JOIN: all rows from the left table, with NULLs where the right has no match — the classic \"list everyone, including those without orders.\" RIGHT JOIN mirrors it. For most reporting, LEFT JOIN is the safe default when you don't want to silently drop rows." },
      { k: ["insert", "add data", "new row", "insert into"], a: "INSERT adds a row: INSERT INTO customers (name, email) VALUES ('Ada', 'ada@example.com');. Match column order to values, or list columns explicitly (always do). Insert many at once: INSERT INTO t (a, b) VALUES (1, 'x'), (2, 'y');. If the table has an auto-increment id, leave it out — the database assigns it. Check the result with SELECT * FROM customers ORDER BY id DESC LIMIT 1;" },
      { k: ["update", "modify data", "change data"], a: "UPDATE changes existing rows: UPDATE customers SET country = 'NG' WHERE id = 7;. The danger: without WHERE, you update EVERY row. Habit: always write the WHERE clause first, then the SET — or run the matching SELECT first to see which rows will change. In production, wrap in a transaction and check the row count after." },
      { k: ["delete", "remove data", "delete row"], a: "DELETE removes rows: DELETE FROM orders WHERE id = 42;. Same danger as UPDATE: DELETE FROM orders; (no WHERE) wipes the whole table. Same habit: write WHERE first, run the SELECT with the same WHERE to verify the rows, then delete. For \"undoable\" deletes, many apps use a deleted_at column (soft delete) instead of removing rows." },
      { k: ["primary key", "unique", "id column"], a: "A PRIMARY KEY uniquely identifies each row — almost always an auto-incrementing id. It can never be NULL, never repeat. Every table should have one; it's the anchor that other tables point to. UNIQUE is like a primary key but allows one NULL and a table can have several UNIQUE columns (email, phone). The rule: if two rows are the same thing, they should share a key, not duplicate." },
      { k: ["foreign key", "relationship", "reference"], a: "A FOREIGN KEY is a column that points to the primary key of another table: orders.customer_id REFERENCES customers(id). It enforces that you can't reference a customer that doesn't exist. That's how relationships live in a relational database: many orders → one customer (many-to-one), which is the most common shape. Lesson 16." },
      { k: ["create table", "ddl", "create database", "drop", "schema design"], a: "CREATE TABLE defines a table: CREATE TABLE customers ( id INTEGER PRIMARY KEY, name TEXT NOT NULL, email TEXT UNIQUE, joined_on DATE );. Each column gets a type (INTEGER, TEXT, REAL, BOOLEAN, DATE/DATETIME) and constraints (NOT NULL, UNIQUE, DEFAULT). DDL (Data Definition Language) is CREATE/ALTER/DROP — the commands that shape the schema itself. Lesson 17." },
      { k: ["index", "performance", "speed up query", "slow query"], a: "An index is a lookup structure (like a book's index) that makes certain queries much faster: CREATE INDEX idx_orders_customer ON orders(customer_id);. It speeds up WHERE, JOIN and ORDER BY on that column — at the cost of slightly slower writes (the index must be updated too). Index the columns you filter and join on most; don't index everything. Lesson 18." },
      { k: ["null", "null values", "is null", "empty", "missing"], a: "NULL means \"unknown / not provided\" — it's not zero, not an empty string, and it behaves unlike anything else: NULL = NULL is not TRUE (it's NULL), so you must write IS NULL / IS NOT NULL to filter. Aggregates skip NULLs, and comparisons with NULL never match. This is the #1 SQL trap — the whole lesson 19 is about it." },
      { k: ["sql project", "shop database", "mini project sql"], a: "The track ends with a shop project: you get a small database (customers, products, orders, order_items) and answer real questions — top products by revenue, customers who never ordered, monthly sales totals. You'll combine SELECT, WHERE, JOIN, GROUP BY and HAVING. It's the capstone: everything from the 19 lessons in one realistic task." },
      { k: ["sqlite", "postgres", "mysql", "which database", "database choice"], a: "They all speak SQL but differ in scale and features: SQLite is a file (no server) — perfect for learning, apps, and small projects; PostgreSQL is the full-featured open-source default for serious apps; MySQL/MariaDB is the classic web database; SQL Server and Oracle are enterprise. For learning, the syntax you practice here (SELECT, WHERE, JOIN, GROUP BY) works in all of them." },

      /* ---------- Git ---------- */
      { k: ["git", "version control", "what is git", "learn git", "git track"], a: "Git records every change to your files, so you can go back, compare, and work in parallel without losing anything. The mental model: working files → staging area → commits (snapshots) → branches (parallel timelines) → remotes (GitHub). The track has 20 lessons from git init to a full team workflow. Install it once: download from git-scm.com, verify with git --version." },
      { k: ["git config", "git init", "first setup", "configure git"], a: "One-time setup: git config --global user.name \"Your Name\" and git config --global user.email \"you@example.com\" — these label your commits. To start tracking a folder: cd into it and git init. Check your state anytime with git status. From here, the loop is: edit files → git add → git commit, repeat." },
      { k: ["git add", "stage", "staging", "staging area"], a: "git add moves changes from your working folder into the staging area — the \"prepare this snapshot\" step. git add file.py stages one file, git add . stages everything changed. Staging lets you commit logically (\"just the login fix\") instead of dumping all changes at once. If you stage something by mistake, git restore --staged file.py un-stages it (your edits stay)." },
      { k: ["git commit", "commit message", "commit"], a: "git commit -m \"Add login form\" takes the staged changes and saves them as a permanent snapshot with your message. Good messages are short, present-tense and explain what changed: \"Fix off-by-one in loop\" beats \"fix\". Each commit records who, when, and why — that history is what makes git valuable. You can amend the last one with git commit --amend." },
      { k: ["git log", "history", "see commits"], a: "git log shows your commit history (newest first). git log --oneline is the compact one-line-per-commit view; --graph adds branch lines. Click any commit hash in a GUI (GitHub, VS Code) to see exactly what it changed. Knowing how to read the log is 50% of debugging \"who broke this and when\"." },
      { k: ["git status", "check state", "what changed"], a: "git status is your compass: it shows what's staged, what's modified but unstaged, and what's untracked (new files git doesn't know about yet). Run it after every few commands — it's free, it's instant, and it prevents almost every \"wait, where is my change?\" panic. If anything looks wrong, status tells you before you commit." },
      { k: ["branch", "branches", "branching", "main branch"], a: "A branch is a parallel timeline of commits. You branch off main to try something without touching the working code: git branch feature or git switch -c feature. Work and commit there; when it's done, merge it back. main stays clean and always deployable. Branch names in kebab-case: feature/login, fix/header-bug." },
      { k: ["git switch", "checkout", "switch branch", "git checkout -b"], a: "git switch -c name creates AND switches to a new branch; git switch name moves to an existing one; git switch main goes back. (git checkout -b is the older spelling you'll still see everywhere — switch is the modern, clearer command.) Your uncommitted changes travel with you when you switch, which is sometimes a surprise — commit or stash first if they matter." },
      { k: ["merge", "git merge", "merge conflict", "combining branches"], a: "git merge brings one branch's commits into the current one. If both branches changed different parts, it's a fast, automatic merge. If both changed the same lines, git stops and marks a CONFLICT — you open the file, see both versions, edit it to the correct result, git add it, and git commit to finish. Conflicts are normal and not scary; they're git doing its job of not guessing. Lesson 12." },
      { k: ["conflict", "resolve conflict", "conflict markers", "<<<<<<"], a: "When a merge conflicts, the file shows markers: <<<<<<< HEAD (your version) / ======= (the other branch) / >>>>>>> branch. You edit out the markers, keeping (or combining) the code you want, then git add <file> and git commit. Read both versions carefully — the fix is often \"take theirs, plus my one line.\" Practice on a toy file first; it's the scariest-sounding part of git and it's actually a text edit." },
      { k: ["gitignore", ".gitignore", "ignore files", "node_modules"], a: ".gitignore is a file (at the repo root) listing patterns git should never track: node_modules/, .venv/, __pycache__/, *.log, .env. The last one matters: .env usually holds secrets, and you do NOT want secrets in your history. Adding it early saves pain — files already tracked can't be ignored retroactively without extra steps. Lesson 13." },
      { k: ["github", "remote", "remote repository", "push to github"], a: "GitHub is a hosting site for git repos: a remote is the copy up there (usually named origin). Create a repo on GitHub, then git remote add origin <url>, git push -u origin main to publish. From then on, git push uploads your commits, git pull downloads others'. The -u sets the upstream so later pushes are just git push. Lessons 15-17." },
      { k: ["clone", "git clone", "download repo", "fork"], a: "git clone <url> downloads an entire repo (history included) into a new folder — the standard way to start contributing or setting up a project on a new machine. A fork is your own copy of someone else's repo on GitHub (one button), which you then clone. Flow for contributing: fork → clone → branch → commit → push to your fork → open a pull request." },
      { k: ["pull", "git pull", "fetch", "update local"], a: "git pull = fetch (download new commits from the remote) + merge (bring them into your branch). Run it before you start work and before you push, so you're building on the latest code. If it conflicts (you and a teammate edited the same lines), resolve as with any merge. git fetch alone is safer when you want to see what's new without changing your working files." },
      { k: ["push", "git push", "publish", "upload"], a: "git push uploads your local commits to the remote. First push to a branch: git push -u origin branch-name (sets upstream); after that, git push is enough. If push is rejected, someone pushed first — git pull to integrate their changes, resolve any conflicts, then push again. Your local commits are safe until you push; the remote is the shared truth." },
      { k: ["pull request", "pr", "merge request", "review"], a: "A Pull Request (PR) is the team's way of saying \"I'm ready to merge this branch — please review.\" You open it on GitHub: it shows your commits and the diff, teammates review and comment, you fix and push more, and once approved, someone merges. PRs are where code quality happens — and where you learn by reading others' code. Lesson 17." },
      { k: ["reset", "checkout", "revert", "undo", "git undo"], a: "Three undo tools: git restore <file> discards uncommitted changes to a file; git reset --soft HEAD~1 undoes the last commit but keeps the changes staged (fix a bad commit message); git revert <hash> adds a NEW commit that undoes an old one (safe for shared history). Rule: never rewrite history on a branch others use — revert instead. When in doubt, ask: is this commit shared or not?" },
      { k: ["stash", "git stash", "temporary changes"], a: "git stash temporarily shoves your uncommitted changes away so you can switch branches or pull without a mess: git stash, do your thing, git stash pop to bring them back. It's the \"let me park this for a minute\" command. If you have several stashes, git stash list shows them and git stash pop stash@{1} picks a specific one." },
      { k: ["tag", "release", "version", "v1.0"], a: "A tag marks a specific commit with a name — usually a release: git tag v1.0.0, then git push --tags to publish. Tags don't move (unlike branches), so v1.0.0 always points at the exact code that was released. That's how teams say \"this is the version in production\" and how users download a specific release from GitHub." },
      { k: ["team", "workflow", "work together", "collaborate", "multiple people"], a: "The standard team flow: everyone works on main except for features; each feature gets its own branch; work in small commits; push early and often; open a PR when ready; review, fix, merge; delete the branch. Small commits + frequent pulls = fewer conflicts. The Git track's final lessons (18-20) walk through fork/contribute and a realistic team scenario end to end." },
      { k: ["git track", "git lessons"], a: "The Git track has 21 lessons: version control basics, install and config, git init, staging, committing, history, status, branches, switching, merging, conflicts, .gitignore, remotes and GitHub, clone/pull, push and pull requests, undoing things, stashing, tags/releases, fork/contribute, team workflow, and a final project: version a real project from day one." },

      /* ---------- cross-cutting ---------- */
      { k: ["which language", "best language", "first language", "should i learn"], a: "It depends on the goal: web pages → HTML/CSS/JavaScript (build what you see); scripts/data/AI → Python; data in apps → SQL (you'll use it regardless); professional workflow → Git (you'll use it regardless). For a first language, Python and JavaScript are the two best starting points — both beginner-friendly and everywhere. Start with the Programming track first, then pick by goal." },
      { k: ["html vs css vs js", "difference html css javascript"], a: "One page, three jobs: HTML is the structure (what's on the page — headings, images, buttons), CSS is the style (how it looks — colors, layout, motion), JavaScript is the behavior (what happens when you click, type, scroll). A building analogy: HTML is the frame and walls, CSS is the paint and furniture, JavaScript is the electricity and plumbing." },
      { k: ["frontend", "backend", "full stack", "fullstack"], a: "Frontend is what users see and touch: HTML, CSS, JavaScript in the browser. Backend is the server side: logic, databases, APIs — often Python, SQL, and a framework. Full stack means doing both. This site covers the frontend deeply (HTML/CSS/JS) plus two backend-adjacent skills (Python, SQL) and the professional glue (Git)." },
      { k: ["how long", "how long to learn", "time", "fast", "timeline"], a: "Honest answer: 20 focused minutes a day, doing the lessons AND the practical tasks, gets you building simple pages in a few weeks and comfortable with the basics in 2-3 months. The bottleneck is never reading — it's doing. Finish each lesson's task and challenge, use the playground daily, and build one small project per track. Consistency beats intensity." },
      { k: ["project", "what to build", "ideas", "portfolio"], a: "Build in this order: a personal page (HTML), a styled version of it (CSS), an interactive quiz or to-do app (JavaScript), a command-line game (Python), a report on a public dataset (SQL), and then a real project committed from day one (Git). Each project reuses the previous track — that's when learning sticks. The final lesson of every track is a project like this." }
    ]
  }
};
