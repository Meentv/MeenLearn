/* MeenLearn · lessons 4/4 — Terminal (20) + Web & Hosting (20) */
window.MEEN_LESSONS_4 = {

  terminal: [
    { t: "What a terminal is (and why you need it)",
      needs: ["terminal"],
      text: "A terminal is a text window where you control your computer by typing commands instead of clicking. Where the desktop gives you icons, the terminal gives you the raw tools underneath: move here, create that file, run this program. Every developer tool you'll ever use — Git, Python, Node, hosting — is operated from a terminal. It looks intimidating until you learn your first five commands, and then it becomes the fastest way to do almost anything on your computer.",
      def: "A terminal (command line interface) is a program that accepts typed commands and shows text output — the direct way to control files, programs and your system.",
      code: "GUI:    [icon] -> [icon] -> [menu] -> click\n\nTERMINAL:\ncd projects\npython app.py\n\nSame actions. No mouse.\nThe terminal is a conversation:\nyou type a command, it shows the\nresult, you get a prompt again.",
      out: { term: "$ cd projects\n$ python app.py\nHello from my app!\n$\n\nThe $ is the prompt:\n\"the computer is waiting\nfor your next command.\"" },
      flow: ["You type a command", "Computer executes it", "Shows text output", "Prompt returns"],
      task: "Open your terminal (next lesson shows how on each OS). Type: echo hello meen. Read the output. You have just run your first program.",
      mistake: "Thinking the terminal is a programming language. It isn't — it's a place to TALK to your computer. The words you type are commands (cd, ls, python), and they're the same on every project you'll ever build.",
      tip: "The prompt is a turn-taking signal: when you see it, the computer is finished and waiting. Commands run in split seconds; if you don't see the prompt come back, something is still running (Ctrl+C stops it — you'll meet that soon).",
      challenge: "You clicked through 6 dialogs to make a folder inside a folder inside a folder. Write the terminal version in your head (one line). Which part of that sentence — 'inside a folder inside a folder' — do you need to learn next? (Hint: lesson 3.)" },

    { t: "Opening your terminal (macOS, Windows, Linux)",
      needs: ["terminal"],
      text: "You already have one — it's built into your computer. macOS: Terminal (Spotlight: press Cmd+Space, type 'Terminal'). Windows: PowerShell or Command Prompt (type 'powershell' in the Start menu search) — VS Code also has a built-in one (Terminal menu → New Terminal, or the ` key). Linux: gnome-terminal, konsole, or xterm. Pick ONE and stick with it — the commands in this track work in all of them (with two or three name differences noted where they matter).",
      def: "The built-in command-line program on your OS: Terminal (macOS), PowerShell/cmd (Windows), or a terminal emulator (Linux) — all speak the same commands taught here.",
      code: "macOS:   Terminal.app\n          (Cmd+Space -> Terminal)\n\nWindows: PowerShell\n          (Start -> powershell)\n\nLinux:   gnome-terminal / konsole\n\nBONUS: VS Code\n  Terminal menu -> New Terminal\n  or press ` (backtick)\n  -> terminal opens INSIDE your\n     project folder, automatically",
      out: { term: "Windows (PowerShell):\nPS C:\\Users\\meen> echo hello\nhello\nPS C:\\Users\\meen>\n\nmacOS (Terminal):\nmeen@laptop ~ % echo hello\nhello\nmeen@laptop ~ %\n\nDifferent prompt style, same\necho command. You're in." },
      flow: ["Find your terminal", "Open it", "Read the prompt (where you are)", "type your first command"],
      task: "Open your terminal, type echo (your name), then type pwd (or cd on Windows) to see where you are. Screenshot it — that's your day-one terminal.",
      mistake: "Installing some random 'terminal app' from the internet when your OS already has a perfectly good one built in. The built-in one is maintained by your OS maker — start there, and add tools later only when you have a specific need.",
      tip: "The prompt tells you WHERE you are: 'C:\\Users\\meen' on Windows, '~' (your home folder) on macOS/Linux. Before doing anything that touches files, read the prompt. It's the single habit that prevents 90% of 'where did that file go?' panic.",
      challenge: "On Windows, 'dir' lists files; on macOS/Linux it's 'ls'. Why do the same tools have different names on different systems? (Hint: history — DOS vs Unix.) What's one command that's spelled the same on all three (test: echo, cd, mkdir)?" },

    { t: "Your current location: pwd and cd",
      needs: ["terminal"],
      text: "Your terminal is always standing inside one folder, and cd (change directory) is how you move. pwd (print working directory) tells you where you stand. Folders nest like this: your computer's root contains folders, which contain folders. 'cd projects' walks into the projects folder; 'cd ..' walks back out to the parent; 'cd ~' takes you home. Think of it like walking through doors: every cd is a door you walk through.",
      def: "pwd prints your current folder; cd <path> moves into a folder (cd .. moves up one level, cd ~ moves to your home).",
      code: "FOLDERS NEST:\n\n  meen/                    <- home (~)\n    projects/\n      meenlearn/\n        index.html\n      notes/\n\n$ pwd\n/home/meen\n\n$ cd projects\n$ pwd\n/home/meen/projects\n\n$ cd meenlearn\n$ cd ..          # back to projects\n$ cd ~           # home",
      out: { term: "$ pwd\n/home/meen\n\n$ cd projects\n$ pwd\n/home/meen/projects\n\n$ cd meenlearn\n$ pwd\n/home/meen/projects/meenlearn\n\n$ cd ..\n$ pwd\n/home/meen/projects\n\nThe path is just 'the folders\nyou've walked through, joined'." },
      flow: ["pwd = where am I", "cd folder = walk in", "cd .. = walk out", "cd ~ = go home"],
      task: "In your terminal: pwd, then cd into a folder you have (Documents or Projects), pwd again, cd .., and confirm you're back. Then cd ~ and pwd. How many doors did you walk through?",
      mistake: "cd into a folder you don't have — the terminal says 'No such file or directory' and you stay where you were (nothing broke; you just didn't move). Always pwd after a confusing cd — it tells the truth instantly.",
      tip: "Pressing the Tab key completes folder names for you: type 'cd pro' + Tab and it fills in 'projects/'. Once you use tab completion, you'll never type a full path again — and typos mostly disappear.",
      challenge: "You're in /home/meen/projects/meenlearn and want to get to /home/meen/notes in as few cd commands as possible. Write it two ways: (a) walking up then in, (b) jumping straight there. Why does (b) work in one command? (Hint: paths can be absolute, starting from / or C:\\.)" },

    { t: "Seeing what's around: ls and dir",
      needs: ["terminal"],
      text: "ls (list) shows what's in your current folder — the terminal's equivalent of opening a folder window. Add flags for more: ls -a shows hidden files (ones starting with . — that's where .git and .env hide), ls -l shows details (sizes, dates, permissions). On Windows, the command is dir. Knowing what's around you before you move or delete anything is the same courtesy as looking before you cross the street.",
      def: "ls (macOS/Linux) / dir (Windows) lists the contents of the current folder; -a shows hidden files, -l shows details.",
      code: "$ ls\nindex.html  style.css  script.js\n\n$ ls -a\n.git  .gitignore  index.html  style.css\n\n$ ls -l\n-rw-r--r--  index.html   9.5K\n-rw-r--r--  style.css    24K\n\ndr = folder, - = file\nWindows:  dir  (dir /a for hidden)",
      out: { term: "$ ls\napp.py  data  README.md\n\n$ ls -a\n.git  app.py  data  README.md\n\n$ ls -l\ndrwxr-xr-x  data\n-rw-r--r--  app.py  1.2K\n\n'.' at the start = hidden file.\n.git is a folder: your repo's\nentire history lives there." },
      flow: ["ls = open the folder", "-a = show hidden", "-l = show details", "look before you act"],
      task: "In your home folder: ls, then ls -a. Find at least two hidden items (they start with a dot). Now cd into a project you have and ls -l — which entry is the biggest file, and how do you know (which column)?",
      mistake: "Trusting the ls output over the prompt: you ls a folder, see files, then act — but if you cd'd somewhere unexpected, you're looking at the wrong folder. The safe order is always pwd → ls → act.",
      tip: "ls -a is the command that reveals your project's 'secret' files: .git (history), .gitignore, .env (secrets), .vscode (editor settings). When something about a project feels invisible, ls -a is where it's hiding.",
      challenge: "A folder shows 5 files with ls but 9 with ls -a. What are the extra 4 likely to be (name the usual suspects for a web project), and which one of them must never be committed to Git?" },

    { t: "Creating things: mkdir, touch, and your first files",
      needs: ["terminal"],
      text: "mkdir makes a folder (make directory); touch creates an empty file (or updates a file's timestamp if it exists). Together they're how you build project structure from nothing: mkdir -p meenlearn/src makes a folder AND any missing parents in one command (-p = parents, no error if they exist). This is the 'skeleton' move — do it in the terminal and you'll never again copy-paste an empty folder structure.",
      def: "mkdir creates folders (-p creates the whole chain); touch creates an empty file; together they build project structure from the command line.",
      code: "$ mkdir -p meenlearn/src/css\n$ cd meenlearn\n$ touch index.html style.css\n$ touch src/app.js\n\n$ ls -a\nindex.html  src  style.css\n\n$ ls src\napp.js\n\nmkdir -p = 'make, including\nparents, quietly if they exist'.\nThe skeleton in 3 commands.",
      out: { term: "$ mkdir -p meenlearn/src/css\n$ cd meenlearn\n$ touch index.html\n$ touch css/style.css 2>/dev/null\n$ mkdir src && touch src/app.js\n\n$ ls -a\n.  ..  index.html  src  style.css\n\n$ find .\n.\n./index.html\n./src\n./src/app.js\n./style.css\n\nThe tree, from zero, in seconds." },
      flow: ["mkdir -p the structure", "cd in", "touch each file", "ls to confirm"],
      task: "Build this structure from your home folder in the terminal: projects/site/ containing index.html, css/style.css, and js/app.js. Count your commands — the best answer is 3 (one mkdir -p, one cd, then touches... or can you do it in 2? Try.).",
      mistake: "mkdir meenlearn when meenlearn already exists — you get 'File exists' and no damage done (unlike some commands, mkdir is safe to retry; that's why -p exists as the 'quiet, safe' version). When in doubt, use mkdir -p.",
      tip: "touch is the fastest way to create a file, but to ADD content you need an editor (VS Code) or echo (next-next lesson). The professional flow: touch to create the file, code to open it — or open the folder in VS Code (code .) and let the editor create files as you go.",
      challenge: "You need 20 empty files named day01.txt to day20.txt in a practice folder. Do it by hand (20 touches) — slow. Now find the loop: for i in {1..20}; do touch day$(printf %02d $i).txt; done. What did that one line do, and why is 'the terminal can loop too' a big deal? (You're using a shell loop — no programming language installed.)" },

    { t: "Moving, copying, deleting: mv, cp, and the rm warning",
      needs: ["terminal"],
      text: "mv moves (or renames) files: mv old.txt new.txt renames; mv file other-folder/ moves. cp copies (the original stays): cp file backup.txt. And then there's rm — delete. rm file removes it, with NO trash bin, NO undo, NO 'are you sure?'. rm -rf folder/ deletes a folder and everything inside it, instantly. This asymmetry — gentle commands, one brutal one — is why you always ls before you rm, and why the rest of your career you'll type rm slowly.",
      def: "mv moves/renames, cp copies, rm deletes — and rm has no undo. rm -rf deletes a folder tree permanently; ls before every rm.",
      code: "$ mv draft.txt final.txt     # rename\n$ mv final.txt archive/      # move into folder\n\n$ cp final.txt backup.txt    # copy (both exist)\n\n$ rm backup.txt              # DELETE. No trash.\n$ rm -rf old-project/        # folder + everything\n\nRULE: ls before rm.\nALWAYS. Every time. Forever.",
      out: { term: "$ ls\ndraft.txt\n\n$ mv draft.txt final.txt\n$ ls\nfinal.txt\n\n$ cp final.txt backup.txt\n$ ls\nbackup.txt  final.txt\n\n$ rm backup.txt\n$ ls\nfinal.txt        # gone. no undo.\n\nThe asymmetry to remember:\nmv/cp are gentle. rm is not." },
      flow: ["mv = move or rename", "cp = copy (original stays)", "rm = delete, NO undo", "ls before every rm"],
      task: "Create a scratch folder. Touch three files, rename one (mv), copy another (cp), then delete the copy (rm). At each step, ls and read what changed. Now: which command made the change you CAN'T undo, and what habit protects you from it?",
      mistake: "rm -rf with a wildcard typo (rm -rf build/* is fine; rm -rf $VAR/ where $VAR is empty becomes rm -rf / — the infamous disaster. Modern shells guard against this, but the lesson stands: expand and read what a wildcard will touch before you hit Enter — ls the pattern first.)",
      tip: "The muscle memory to build: before any rm, your finger also types ls <the-thing> so you SEE exactly what's about to go. It costs two seconds; the alternative costs a project. And for anything precious, cp a backup first — copying is free, losing isn't.",
      challenge: "You need to keep 'report-v2.pdf' but also make a copy called 'report-v2-final.pdf'. One command? (cp.) Now you realize the ORIGINAL was a typo — you actually want to RENAME v2 to final. One command? (mv.) Why is it safer to think 'rename' (mv) than 'copy then delete' (cp + rm)?" },

    { t: "Navigating like you know the place",
      needs: ["terminal"],
      text: "The shortcuts that make the terminal feel fast: . is the current folder, .. is the parent, ~ is your home, / is the root (C:\\ on Windows). You can mix them in any path: cd ~/projects/site, cd ../../Documents. And tab completion fills in the rest — type two letters, press Tab. Once these are reflexes, navigation takes less than a second per hop, and 'the terminal is slow' disappears from your vocabulary forever.",
      def: "Path shortcuts: . (here), .. (up one), ~ (home), / (root) — combinable in any path; Tab completes names as you type.",
      code: ".        current folder\n..       parent folder\n~        your home\n/        root  (C:\\ on Windows)\n\nMix them freely:\ncd ~/projects/site\ncd ../../Documents\ncd /tmp        (scratch space)\n\nTAB = autocomplete:\n  type 'cd pro' + TAB -> projects\n  type 'py' + TAB     -> python\n  press TAB twice     -> show options",
      out: { term: "$ cd ~/projects\n$ pwd\n/home/meen/projects\n\n$ cd ../..\n$ pwd\n/home\n\n$ cd /tmp\n$ pwd\n/tmp\n\n$ cd me   <- type 3 letters + TAB\n$ cd meenlearn\n(TAB filled 'meenlearn' for you)" },
      flow: [". / .. / ~ / /", "combine into paths", "Tab completes", "navigation = reflexes"],
      task: "From anywhere: get to your home with one command (~), then to a project folder using a combined path (~/...), then back home with .. hops only. Now practice Tab: type the first two letters of a folder + Tab, three times. Time yourself — beat 10 seconds total.",
      mistake: "Typing full absolute paths from memory (cd /home/meen/projects/site/css) — error-prone and slow. Relative shortcuts (~/, ..) plus Tab get you there with fewer keystrokes and self-correcting (Tab won't complete a folder that doesn't exist).",
      tip: "cd - takes you back to your PREVIOUS folder (like 'back' in a file manager). Paired with ls, the daily navigation loop is: cd - → ls → cd <target>. Three commands, zero maps.",
      challenge: "You're in /a/b/c/d and need /a/b/e. Write the shortest path (hint: go up twice, then in — ../.. then e, or the combined form). Now the trickier one: you're in /a/b/c and need /a/b/c/d/e/f — you can't 'walk into' folders that don't exist. What command CREATES the whole chain and walks you there in one move? (You learned it two lessons ago.)" },

    { t: "Reading files: cat, less, head, tail",
      text: "Four commands for reading what's inside a file: cat file shows the whole thing (great for short files); less file opens a scrollable view (press q to quit — this trips everyone up the first time); head file shows the first 10 lines (head -5 for five); tail file shows the last 10 (tail -f is the superpower: it keeps showing NEW lines as a file grows — how you watch a program's logs live).",
      def: "cat prints a whole file; less pages through it (q quits); head shows the top; tail shows the bottom — and tail -f follows a growing file live.",
      code: "$ cat notes.txt        # whole file\n$ head -5 log.txt      # first 5 lines\n$ tail log.txt         # last 10 lines\n\n$ less bigfile.txt     # scroll\n  (space = down, q = QUIT)\n\nTHE SUPERPOWER:\n$ tail -f server.log\n  (keeps printing new lines\n   as they appear - Ctrl+C to stop)\n\n'cat' = concatenate & print.\nIt just prints the file.",
      out: { term: "$ cat recipe.txt\n1. boil water\n2. add coffee\n3. pour\n\n$ tail -3 recipe.txt\n2. add coffee\n3. pour\n\n$ echo \"4. drink\" >> recipe.txt\n$ tail -f recipe.txt   # watch it live\n4. drink\n^C  (stopped)" },
      flow: ["cat = all of it", "head/tail = edges", "less = scroll big files", "tail -f = live logs"],
      task: "Create a text file with 15 lines (echo is fine, or VS Code). Read it with cat, head -5, tail -3, and less (find the q key on purpose — don't panic). Then run tail -f in one window and add a line from another — watch it appear by itself.",
      mistake: "Getting stuck inside less and thinking the terminal froze. It hasn't — less is waiting for you. The key is q. (Also: if a command seems to hang, Ctrl+C stops it; that exit-hatch exists for everything.)",
      tip: "tail -f is the developer's live-view: run your app so it writes a log file, tail -f that log in another window, and you're watching your program think in real time. This one habit replaces a surprising amount of 'why did that just happen?' guessing.",
      challenge: "You need to see ONLY lines 6 through 10 of a 100-line file. Which combo of the tools you know does it (hint: head -10 then... something)? (If you know sed/awk, those do it in one — but the head+tail thinking is the skill.) What's the equivalent question for a LOG: 'show me the 20 lines right before the crash'?" },

    { t: "Writing text from the terminal: echo, > and >>",
      needs: ["terminal"],
      text: "echo prints text — and with > or >> it writes files. echo \"hello\" > file.txt creates the file with that line (and WIPES any old content — > means overwrite); echo \"more\" >> file.txt appends (>> means add to the end). This is how scripts create config files, and how you'll quickly jot notes without opening an editor. One warning lives here: > is destructive, exactly like rm — when in doubt, peek with cat first.",
      def: "echo prints text; > writes it to a file (overwriting), >> appends. The difference between one and two angle brackets is the difference between 'replace' and 'add'.",
      code: "$ echo \"hello\" > note.txt     # create/overwrite\n$ cat note.txt\nhello\n\n$ echo \"world\" >> note.txt    # append\n$ cat note.txt\nhello\nworld\n\n$ echo \"danger\" > note.txt    # OVERWRITE!\n$ cat note.txt\ndanger\n\n>  = replace the whole file\n>> = add to the end\n\nSame arrow, one bracket = risk.",
      out: { term: "$ echo \"line one\" > list.txt\n$ echo \"line two\" >> list.txt\n$ echo \"line three\" >> list.txt\n$ cat list.txt\nline one\nline two\nline three\n\n$ echo \"START FRESH\" > list.txt\n$ cat list.txt\nSTART FRESH   (the other 3 lines: gone)" },
      flow: ["echo the text", "> = write (overwrite)", ">> = append", "cat to verify"],
      task: "Build a file called grocery.txt with 4 items using one > and three >> commands. cat it. Now echo a 5th item with > instead of >> — what happened to the list? Rebuild it correctly. Feeling the difference between > and >> is the whole lesson.",
      mistake: "Using > when you meant >> (the file's history vanishes — 'where did my config go?' is almost always this) and forgetting quotes around text with spaces or special characters (echo $HOME > file works; echo a b c > file also works but echo \"a b\" >> x is safer). When your text has spaces, quote it.",
      tip: "echo is also how you build files line by line in scripts — and how you test what a command will do before committing it: echo rm -rf old/ prints the command instead of running it. The pattern 'echo it first' is a safe-preview habit worth keeping.",
      challenge: "Write a 5-line file in ONE command (hint: echo with newlines inside the quotes, or multiple echoes chained with &&). Then answer: if a script does echo $API_KEY > .env and .env already existed with other settings — what just happened, and what's the safe version of that line?" },

    { t: "The anatomy of a command: programs, flags, and arguments",
      needs: ["terminal"],
      text: "Every command has the same shape: PROGRAM + FLAGS + ARGUMENTS. The program is what runs (ls, git, python). Flags (starting with -) tweak its behavior (-a for all, -l for long). Arguments are the things it acts on (filenames, text). ls -la is: program ls, flags -l and -a combined, no arguments. Once you see this shape, you can read any command you'll ever meet — including the ones in error messages and Stack Overflow answers — like a sentence with a grammar.",
      def: "Command anatomy: program (what to run) + flags (how: -short, --long) + arguments (what to run it on). The same shape for every tool on your computer.",
      code: "  program  flags     arguments\n  --------  -------   ----------\n  ls        -la\n  git       commit    -m \"msg\"\n  python    -u        app.py\n  echo      (none)    \"hello\"\n  mkdir     -p        my/folder\n\nFLAGS start with - (or --):\n  -a all, -l long, -p parents\n  --help = 'explain yourself' (works\n  on almost EVERY program!)\n\nArguments = the nouns. Flags =\nthe adverbs. Program = the verb.",
      out: { term: "$ git commit -m \"Add index\"\n     |      |      |----------\n     |      |      + argument\n     |      +------- flag\n     +--------- program\n\n$ ls -la\n  |   ^\n  |   + combined flags (-l AND -a)\n  + program, no arguments\n\n$ python --help\n(flags in --long form, and\n--help is your built-in manual)" },
      flow: ["program = the verb", "flags = -a -l --help", "arguments = the nouns", "read it like a sentence"],
      task: "Pick three commands you've used this track (ls, cd, echo, mkdir, git...). For each, label the program, flags, and arguments out loud. Then run git --help (or ls --help) and find the flag you didn't know existed. You now read commands the way developers do.",
      mistake: "Swapping flag and argument order where it matters (some programs are strict: git commit -m \"msg\" works, git commit \"msg\" -m doesn't) and assuming flags are universal (-a means 'all' in ls but different things elsewhere). When a command misbehaves, its --help is the source of truth, not your memory.",
      tip: "--help (or -h) is the most underused command in computing: it works on nearly every program and prints that program's own manual. Before Googling a tool's flags, run tool --help — you'll know in 10 seconds what you'd spend 10 minutes finding.",
      challenge: "Decode without a manual: python -m http.server 8000 --bind 0.0.0.0. Which part is the program, which is the flag that says 'run this module as a script', which is the module name, and what do the last two arguments set? (If you ran it — congratulations, you served a website. That's also a Web-track lesson.)" },

    { t: "Running programs and your own code",
      needs: ["terminal"],
      opt: ["vscode"],
      text: "The terminal is where code actually runs. python app.py runs a Python file; node app.js runs a JavaScript file (Node track: Web lesson 15); ./script.sh runs a shell script. The pattern is always PROGRAM file: the program (interpreter) does the thinking, the file is what it thinks about. 'command not found' means that program isn't installed (or not on your PATH — lesson 13). This one screen is where your Python games, your Git workflow, and your deploys all actually happen.",
      def: "You run code by naming its interpreter: python file.py, node file.js — 'program + file'. 'command not found' means the program is missing or not on PATH.",
      code: "$ python game.py      # run Python\n$ python              # (no file) =\n                       #  interactive REPL\n\n$ node script.js      # run JS\n  (needs Node.js installed)\n\n$ ./run.sh            # run a shell\n                       #  script directly\n\n'command not found'?\n -> not installed, or not on PATH\n    (next-next lesson)\n\nThe terminal is where code RUNS.\nEditors write it; the terminal\nruns it.",
      out: { term: "$ echo 'print(\"hi from the terminal\")' > hi.py\n$ python hi.py\nhi from the terminal\n\n$ python\n>>> 6 * 7\n42\n>>> exit()\n\n$ node --version    (if installed)\nv20.x\n\nThree run modes: a file, the REPL\n(interactive), a version check." },
      flow: ["write the file (editor)", "terminal: program + file", "read the output", "fix, re-run"],
      task: "Create hello.py in the terminal (echo 'print(\"hello\")' > hello.py — one command, two skills), run it with python, then run it again from a DIFFERENT folder using its full path. What changed between the two runs, and what does that teach you about 'where you are' when you run things?",
      mistake: "Running python file.py and getting 'SyntaxError' when the file was fine — often the file wasn't saved, or you're in the wrong folder (the interpreter can't find a file that isn't where you told it). The debug pair: pwd + ls first, always.",
      tip: "The dev loop, internalized: edit (VS Code) → save → terminal: run → read output → fix → run. It's the same loop in every language and every project of your career. The terminal is the 'run' in that loop — make it comfortable now.",
      challenge: "You wrote a script called run.py and 'python run.py' says the file doesn't exist — but you JUST made it. List the four things to check (in order of likelihood) before you assume the universe is broken. (One of them is a folder you're not in; one is a save you didn't make; two more are subtle.)" },

    { t: "Installing software the safe way",
      needs: ["terminal"],
      text: "Your OS has a package manager that installs programs safely: Homebrew (macOS: brew install git), winget or Chocolatey (Windows: winget install Python.Python.3.12), apt (Debian/Ubuntu: sudo apt install ...). On top: pip installs Python packages (pip install requests), npm installs JavaScript packages (npm install express). The safe pattern is always: install into a project's own environment (venv / node_modules) rather than the whole machine, and only run install commands from sources you trust (official sites, known packages).",
      def: "OS package managers (brew, winget, apt) install programs; pip and npm install language packages. Install into project environments where possible, from sources you trust.",
      code: "macOS:     brew install git\nWindows:    winget install OpenJS.NodeJS\nLinux:      sudo apt install git\n\nLanguage packages (inside a project):\n  python -m venv .venv     # sandbox\n  pip install requests     # into sandbox\n  npm install express      # into node_modules\n\nTRUST RULE: official sites + known\npackage names only.\n'Install' is power - use it carefully.",
      out: { term: "$ brew install git\n==> Fetching git\n==> Pouring git--2.45.0.bottle.tar.gz\n🍺  /opt/homebrew/Cellar/git/2.45.0: 1,696 files\n\n$ git --version\ngit version 2.45.0\n\n$ pip install requests\nSuccessfully installed requests-2.32.0\n\nThe same shape everywhere:\nmanager + install + name, then\nverify with --version." },
      flow: ["pick the right manager", "install the package", "verify (--version)", "prefer project sandboxes"],
      task: "Check what you already have: run python --version, git --version, node --version (one of these may say 'not found' — that's information, not failure). Then identify which package manager your OS uses (brew on macOS? winget on Windows? apt on Ubuntu?) and write — don't run — the command to install the thing you're missing.",
      mistake: "Randomly installing from unknown sites or packages with suspicious names ('python-best-boost-v2' is not a real library). The trust check: official domain (python.org, nodejs.org, git-scm.com), or the package name from the documentation of a tool you already trust.",
      tip: "The verify step is non-negotiable: after any install, run the tool's --version. It proves the install landed AND that your terminal can find it (a silent failure of the second kind is 'installed but not on PATH' — lesson 13). Install → verify → move on.",
      challenge: "A tutorial says 'sudo npm install -g some-tool'. Decode it (sudo = run with admin rights, -g = globally for the whole machine, not this project) and explain why the 'safe pattern' from this lesson would do it differently. When IS global + sudo actually the right choice? (Think: git itself.)" },

    { t: "Environment variables and PATH (why 'command not found')",
      needs: ["terminal"],
      text: "Environment variables are named settings your computer keeps for every program. The one that matters daily is PATH: a list of folders your terminal searches when you type a command. Type python and your terminal asks: 'is there a python in any folder on PATH?' If yes, it runs it; if no — 'command not found'. That's why a freshly installed program sometimes 'doesn't work' (it's not on PATH yet — reinstall with 'add to PATH' checked, or restart the terminal). This one concept explains half of all beginner setup mysteries.",
      def: "Environment variables are named settings for programs; PATH is the list of folders the terminal searches to find commands. 'command not found' = not installed, or not on PATH.",
      code: "You type:  python\n\nTerminal asks: 'is python in any\nfolder on my PATH list?'\n\nPATH = /usr/local/bin : /usr/bin : ...\n       (a list of folders, colon-\n        separated; ; on Windows)\n\nFound? run it.\nNot found? 'command not found'.\n\nSee yours:\n  macOS/Linux:  echo $PATH\n  Windows:      echo $env:PATH\n\nSecrets live here too (and in\n.env files - Web lesson 17).",
      out: { term: "$ echo $PATH\n/usr/local/bin:/usr/bin:/bin:/opt/homebrew/bin\n\n$ which python\n/usr/local/bin/python\n\n'which' = 'where would the\nterminal find this command?'\n\n$ echo $HOME\n/home/meen\n(env vars: $NAME to read, on mac/Linux)" },
      flow: ["type a command", "terminal scans PATH folders", "found -> runs it", "not found -> PATH problem"],
      task: "Run echo $PATH (or echo $env:PATH on Windows) and read your list. Then run which python (or where python) to see exactly which folder your terminal found it in. Finally: what's on YOUR path to making a command 'found' after you install a new program? (Two answers — one is a setting, one is a habit.)",
      mistake: "Editing PATH by hand in a text file (one missing colon and half your system stops finding things). The safe routes: the installer's 'add to PATH' checkbox, or your OS's proper env-settings tool. And: after PATH changes, OPEN A NEW terminal — running ones keep the old list.",
      tip: "which <command> (where on Windows) is the diagnostic: it answers 'would the terminal find this, and where?' Before debugging 'not found', run which on the working and broken commands — the difference between their answers usually IS the answer.",
      challenge: "You installed Node, but 'node: command not found' persists in every terminal. Walk the diagnosis: which node (what does it say?), echo $PATH (is Node's folder there?), where did the installer put Node? What's the cleanest fix (hint: it's not editing PATH — it's a setting on the installer or a one-line addition, followed by the habit from this lesson)?" },

    { t: "History, shortcuts, and speed",
      needs: ["terminal"],
      text: "The terminal remembers everything you've typed. Up-arrow scrolls your history; Ctrl+R searches it as you type. The daily shortcuts: Ctrl+C stops the running command, Ctrl+L clears the screen, Ctrl+A/Ctrl+E jumps to line start/end, Tab completes. Master these six and the terminal goes from 'typing' to 'conversing' — most of what you type gets completed or recalled by the machine, not your fingers.",
      def: "History + control shortcuts (↑ for history, Ctrl+R search, Ctrl+C stop, Ctrl+L clear) — the muscle memory that makes the terminal fast.",
      code: "The six that matter:\n  Up arrow   previous commands\n  Ctrl + R   search history (type\n             as you search)\n  Ctrl + C   STOP the running\n             command (your exit hatch)\n  Ctrl + L   clear the screen\n  Ctrl + A   jump to line start\n  Ctrl + E   jump to line end\n\n  TAB        autocomplete\n  TAB TAB    list options\n\nhistory      = your full log\n  (grep it:  history | grep git)",
      out: { term: "$ (run a long command...)\n$ Up Up Up   <- back through history\n$ Ctrl+R g   <- searches 'g...':\n  git log --oneline (3 days ago)\n\n$ python slow_thing.py\n  (it hangs...)\n  Ctrl+C     <- stopped.\n  (KeyboardInterrupt)\n\n$ Ctrl+L     <- fresh screen\n$ _" },
      flow: ["Up = recall", "Ctrl+R = search", "Ctrl+C = stop", "Ctrl+L = clear", "Tab = complete"],
      task: "The muscle-memory drill: run any command 4 times using only the Up arrow (never retype). Find a command from earlier in your session with Ctrl+R. Clear with Ctrl+L. Stop a hanging command with Ctrl+C (start 'python' REPL, type a line, and interrupt it). Six moves, each used at least once.",
      mistake: "Hunting through scrolled-up output to find an old command (Ctrl+R finds it in one keystroke) and letting a stuck command pile up (Ctrl+C is always available — there is no terminal state that a determined Ctrl+C + new window can't escape).",
      tip: "history | grep <word> is the power move: your terminal's search engine over everything you've ever typed. 'What was that git command again?' — history | grep 'git re' finds it. You are your own documentation.",
      challenge: "You need to re-run a command you typed 200 commands ago, but with one word changed. Do it without re-typing (Up/ctrl+R to find it, then edit IN PLACE — which shortcuts move you to the right spot? Ctrl+A/E + arrows, or just retype the one word after deleting to it). What's the name of this skill, and why does it matter more as your command lines get longer?" },

    { t: "Piping: connecting commands with |",
      needs: ["terminal"],
      text: "The pipe (|) sends one command's output into another command's input — commands become LEGO pieces. grep filters lines (grep error log.txt shows lines containing 'error'), wc counts (wc -l counts lines), and pipes chain them: tail -f server.log | grep ERROR shows only new error lines, live. This is where the terminal becomes a programming environment in itself: small tools, composed, solving problems you never had to write a script for.",
      def: "The pipe (|) feeds one command's output into the next; grep filters, wc counts, sort orders — small tools composed into pipelines.",
      code: "grep  = find lines containing a word\nwc    = count (lines -l, words -w)\nsort  = sort lines\nhead  = first N\n\nCOMPOSE THEM:\n\n$ cat access.log | grep 404 | wc -l\n  (all log lines -> only 404s -> count)\n\n$ ls -l | sort -k5 | tail -5\n  (files -> sort by size -> biggest 5)\n\n$ history | grep git\n\nEach tool does one thing. The\nPIPE is what makes them a system.",
      out: { term: "$ echo -e \"error: x\\ninfo: ok\\nerror: y\"\n  | grep error\nerror: x\nerror: y\n\n$ echo -e \"a\\nb\\nc\\nd\\ne\" | wc -l\n5\n\n$ ls -l | grep .css\nthe .css files, nothing else\n\nPipes read left to right:\neach | hands its output to the\nnext command's input." },
      flow: ["command A outputs", "| hands it over", "command B filters/counts", "chain as many as you need"],
      task: "Create a text file with 10 mixed lines (some containing 'warn'). Build three pipelines: count the warn lines (grep + wc -l), show only non-warn lines (grep -v), and show the last 3 lines of the grep result (| tail -3). You've just written your first 'program' — with zero programming language.",
      mistake: "Piping a command that expects FILE arguments into one that reads STDIN (grep error file.txt | wc -l is fine, but some tools behave differently with - vs filenames) — when a pipeline misbehaves, test each piece SEPARATELY (run the first command alone, see its output) before debugging the chain.",
      tip: "The diagnostic pattern: when a pipeline gives a weird result, break it at the pipe — run the left side alone, confirm its output, then add the right side. Pipelines fail at one joint; find the joint.",
      challenge: "You have a 10,000-line log and need: how many lines mention 'timeout', then show those lines sorted by frequency of the next word. Sketch the pipeline in words (which tools, in what order, what each does). You may not know every flag yet — the skill is seeing the SHAPE: filter → transform → count. Which part would you test first if the count looks wrong?" },

    { t: "The terminal + Git: your version control home",
      needs: ["git", "terminal"],
      text: "Git lives in the terminal (GUIs wrap it, but the terminal is native ground). The daily dance: git status (where am I?), git add <files> (stage), git commit -m \"message\" (snapshot), git log --oneline (history), git push (publish). The Git track teaches each command deeply — this lesson is just the map: you'll run these exact commands in the terminal, in this order, hundreds of times, and they'll feel as natural as saving a file.",
      def: "Git is operated from the terminal: status → add → commit → log → push — the daily version-control loop (taught in depth in the Git track).",
      code: "The daily loop, in the terminal:\n\n$ git status          # where am I?\n$ git add index.html  # stage the change\n$ git commit -m \"Add hero section\"\n  [main a1b2c3d] Add hero section\n$ git log --oneline   # the history\n  a1b2c3d (HEAD) Add hero section\n  9f8e7d6 Add styles\n$ git push            # publish\n\nEvery Git command you'll love\n(or fear, at first) is typed\nhere. The Git track is the deep\ndive - this is the map.",
      out: { term: "$ git status\nOn branch main\nChanges not staged:\n  modified: index.html\n\n$ git add index.html\n$ git commit -m \"Add hero section\"\n[main a1b2c3d] Add hero section\n 1 file changed, 12 insertions(+)\n\n$ git log --oneline\na1b2c3d (HEAD -> main) Add hero section\n9f8e7d6 Add styles\n5a4b3c2 Initial structure" },
      flow: ["git status (compass)", "git add (stage)", "git commit (snapshot)", "git log (history)", "git push (publish)"],
      task: "In any folder: git init, touch a file, git add ., git commit -m \"First commit\" (you'll be asked for a name/email the first time — set them with git config --global, Git lesson 2), then git log --oneline. You've now run the full loop in the terminal. The Git track is next.",
      mistake: "Chaining git commands with && without reading the first output (git add . && git commit -m x && git push — if the add staged something you didn't mean, the commit and push sail ahead of you). For the first few weeks: one command, read the output, next command. Speed comes after trust.",
      tip: "git status is free, instant, and never wrong — run it before every other git command when in doubt. Terminal habit + git habit combined: the prompt tells you WHERE, status tells you WHAT STATE. Two reads, zero surprises.",
      challenge: "You commit, push, then realize the commit message was a typo. It's YOURS and unshared... wait, you already pushed. Walk the two options (git commit --amend + force-push vs a new fixing commit), and explain why the terminal makes you FEEL the difference (amend rewrites history you now share — what does 'shared history' concretely mean for a teammate?) (Git lesson 17 goes deep.)" },

    { t: "The terminal + Python: your scripting home",
      needs: ["python"],
      text: "Python's home is the terminal. python file.py runs a script; bare python drops you into the REPL (type, Enter, result — your experimentation room); python -m venv .venv creates a project sandbox; pip install adds packages. The Python track runs every project this way — the editor writes code, the terminal runs it, and the REPL is where you test an idea in 10 seconds before committing it to a file.",
      def: "The Python terminal loop: python file.py to run, bare python for the REPL, venv + pip for project packages.",
      code: "The Python lives in the terminal:\n\n$ python game.py        # run a script\n$ python                # REPL: experiment\n  >>> 6 * 7\n  42\n  >>> exit()\n\n$ python -m venv .venv  # project sandbox\n$ source .venv/bin/activate   (mac/Linux)\n  .venv\\Scripts\\activate       (Windows)\n$ pip install requests\n\nEditor writes. Terminal runs.\nREPL tests. That's the whole\nPython workflow.",
      out: { term: "$ echo 'name = \"Ada\"; print(f\"hi {name}\")' > hi.py\n$ python hi.py\nhi Ada\n\n$ python\n>>> import math\n>>> math.sqrt(144)\n12.0\n>>> exit()\n\n$ python --version\nPython 3.12.4" },
      flow: ["python file.py (run)", "bare python (REPL)", "venv + pip (packages)", "editor writes, terminal runs"],
      task: "The full loop, in the terminal: create a one-line script with echo, run it, then open the REPL and test the same idea interactively, then python --version to confirm your setup. Four commands — that's the daily Python rhythm.",
      mistake: "Running python file.py from a folder that doesn't contain file.py ('can't open file' — pwd + ls first), and on Windows, typing 'python3' when your install registered 'python' (or vice versa — try both; --version on each tells you which one works).",
      tip: "The REPL is a time machine for thinking: paste a tricky line, see the result, THEN write it into your script. If a line works in the REPL, the bug is in how the script uses it — you've just split the problem in half. Professionals live in this loop.",
      challenge: "A script runs fine in the REPL (you paste its lines) but fails as a file. Name the three classic differences (one is indentation that got flattened, one is a line that depends on REPL state you didn't import, one is... the file isn't saved). Which of the three does 'run it again from the REPL line by line' isolate fastest?" },

    { t: "The terminal + Node.js: JavaScript off the browser",
      needs: ["node"],
      text: "Node.js is JavaScript's engine, freed from the browser: node script.js runs JS on your machine, bare node is its REPL (the same type-Enter-result loop as Python's), and npm is its package manager (npm init, npm install <package>). Everything the JS track taught you — variables, functions, arrays, objects — runs here unchanged. Node is also how the web's tooling works: build tools, servers, and this site's entire backend world.",
      def: "Node.js runs JavaScript outside the browser: node file.js to run, bare node for the REPL, npm for packages (npm init, npm install).",
      code: "JavaScript's home, part two:\n\n$ node app.js         # run JS here\n$ node                # REPL: try things\n  > 6 * 7\n  42\n  > \"meen\".toUpperCase()\n  'MEEN'\n  > .exit\n\n$ npm init -y         # start a JS project\n$ npm install express # add a package\n\nSame language as the browser.\nDifferent home. Web lesson 16\ngoes deeper (npm).",
      out: { term: "$ echo 'console.log(\"node works:\", 6*7)' > hi.js\n$ node hi.js\nnode works: 42\n\n$ node\n> [1,2,3].map(x => x*2)\n[ 2, 4, 6 ]\n> .exit\n\n$ npm --version\n10.x" },
      flow: ["node file.js (run)", "bare node (REPL)", "npm init + install", "same JS, new home"],
      task: "If Node is installed: create hi.js with echo, run it, open the REPL and run an array method you know from the JS track (.map or .filter), then npm --version. If Node isn't installed: write down (don't run) the three commands above as your 'when I install it' list — and note that the Web track's lesson 15 explains why you'd want it.",
      mistake: "Assuming browser JS and Node JS are the same environment (they're the same LANGUAGE, different worlds: no document, no window, no localStorage in Node — those are browser things; Node has fs, http, process instead). 'document is not defined' in the terminal is the classic first encounter.",
      tip: "The JS-track ↔ Node connection: everything you practiced (variables, functions, arrays, objects, try/catch) transfers 1:1. When the Web track says 'run it with Node', the only new thing is the command — the thinking is already yours.",
      challenge: "You paste your JS-track to-do app code into a .js file and run it with node — it crashes on document.getElementById. Explain in two sentences what's missing (environment, not code), and what the Node-version of 'the page' would be instead (hint: arrays in memory + a file for saving — which two Node built-ins are the substitutes for the DOM and localStorage?) (Web lesson 16.)" },

    { t: "Reading real error messages",
      needs: ["terminal"],
      text: "Terminal errors look scary because they're dense — but they follow the same rule as Python tracebacks: read from the BOTTOM up. The last line is the actual problem; the lines above show the path that led there. Common vocabulary: 'Permission denied' (you lack rights — or the file isn't marked executable), 'No such file or directory' (wrong path — pwd + ls), 'command not found' (not installed / not on PATH), 'KeyboardInterrupt' (you pressed Ctrl+C — not an error at all). You've already read four of these this track; this lesson names them on purpose.",
      def: "Read terminal errors bottom-up: the last line is the problem, the context above explains the path. Permission denied / No such file / command not found / KeyboardInterrupt are the four you'll meet daily.",
      code: "Read BOTTOM-up:\n\n$ python missing.py\nTraceback (most recent call last):\n  File \"...\", line 1, in <module>\nFileNotFoundError: [Errno 2] No\nsuch file or directory: 'data.txt'\n        ^ the answer is at the bottom\n\nTHE FOUR DAILY DRUIDS:\n  command not found   -> not installed /\n                         not on PATH\n  No such file        -> wrong path\n                         (pwd, ls)\n  Permission denied   -> rights, or run\n                         with ./ (execute bit)\n  KeyboardInterrupt   -> you pressed\n                         Ctrl+C. Not an error.",
      out: { term: "$ ls nothere/\nls: nothere: No such file or directory\n$ pwd            <- where am I, really?\n/home/meen\n$ ls             <- what's ACTUALLY here\nprojects  notes\n\n$ python app.py\n  (hangs...)\n  ^C\nKeyboardInterrupt\n  <- you stopped it. Normal exit.\n\nEvery 'scary' line above had a\ncalm, one-line cause." },
      flow: ["error appears", "read the LAST line first", "name it (the four)", "fix that one thing"],
      task: "Deliberately create three errors: run a command that doesn't exist (frobble), ls a folder that doesn't exist, and run a python file that opens a nonexistent file. For each: read the bottom line, name the error type, and fix it. You've now debugged the terminal on purpose — the fear is gone.",
      mistake: "Reading top-to-bottom and 'fixing' the first thing that looks wrong (the path in line 1 when the real problem is in the last line), and treating every red text as a disaster (KeyboardInterrupt is you; warnings are often notes, not failures). Calm reading beats fast fixing.",
      tip: "The 'not an error' list is worth memorizing: KeyboardInterrupt (you stopped it), deprecation warnings (still works, for now), and 'npm WARN' lines (advice, not failure). Knowing what NOT to fear is half of debugging.",
      challenge: "A teammate says 'it's broken' and shows a 30-line error ending in 'ECONNREFUSED 127.0.0.1:5432'. Decode it without Googling: what kind of thing is this (hint: ECONN + a port number = something tried to CONNECT and nothing was listening), what would you ask the teammate (what command, what machine, what was supposed to be running on that port), and what's your first check (is the database server even started?)" },

    { t: "Project: build a developer setup from zero",
      needs: ["terminal"],
      opt: ["vscode", "git", "python"],
      text: "The capstone, and genuinely useful: build a real developer folder structure in the terminal, the way you'd set up a new computer. The spec: a projects/ folder, inside it a meenlearn-practice/ project with src/ and css/ subfolders, a README.md, a .gitignore, git initialized and committed, and — if you have Python — a hello.py that runs. Every command in this track, one final time, building something you'll actually keep. When it's done, this folder IS your workspace.",
      def: "Build a real developer workspace in the terminal: projects/ tree, a git repo, a .gitignore, a README, a runnable script — the full setup, from zero.",
      code: "SPEC (do it all, in the terminal):\n[ ] mkdir -p ~/projects/meen-practice/src\n[ ] mkdir -p ~/projects/meen-practice/css\n[ ] cd into it\n[ ] touch README.md .gitignore src/app.js\n[ ] echo your name > README.md\n[ ] echo '.venv/' >> .gitignore\n[ ] git init + add + commit\n[ ] (python) echo script + run it\n[ ] git log --oneline reads like a\n    story\n\nOne folder. Zero clicks.\nYour workspace, from the\ncommand line - forever.",
      out: { term: "$ mkdir -p ~/projects/meen-practice/{src,css}\n$ cd ~/projects/meen-practice\n$ touch README.md .gitignore src/app.js\n$ echo \"# Meen practice\" > README.md\n$ echo \".venv/\" >> .gitignore\n$ git init && git add . && \\\n  git commit -m \"Initial structure\"\n$ git log --oneline\nf3e2d1c (HEAD -> main) Initial structure\n\nThe tree, the repo, the first\ncommit - in under a minute." },
      flow: ["mkdir -p the tree", "touch the files", "git init + commit", "run a script", "the log reads like a story"],
      task: "Run the full spec in your terminal, for real. Add two commits of your own (e.g. 'Add app.js placeholder', 'Update README') so the log has a real shape. Then: ls -a to see the .git folder appear, and git status to confirm you're clean. This folder is now yours — put your next real project inside it.",
      mistake: "Building the structure but skipping the .gitignore (then committing .venv or node_modules by accident — the ignore file must exist BEFORE the first commit, Git lesson 12) and committing before git config is set (your commits labeled with a machine username — fix it early, Git lesson 2).",
      tip: "This setup IS the template for every new computer you'll ever use: the same 10 commands, a new machine, and you're productive in five minutes. Write the commands down somewhere you'll find them (the README is a fine place) — future-you says thanks.",
      challenge: "Extend the project: add a second project folder (a 'todo-cli' for the Python track's game), give it its own venv, and confirm the two projects' packages can't see each other. Then answer: what would have gone wrong if you'd installed packages into the machine-wide Python instead (the venv lesson's core question, now felt in your own folders)?" }
  ],

  web: [
    { t: "How the web actually works (the big picture)",
      text: "Here's the whole machine: you type a URL, your browser asks a DNS server for the machine's address, connects to that server, and the server sends back files (HTML, CSS, JS, images). Your browser builds the page from those files. That's it — that's the web. Every website, every app, every API is some version of this exchange. Once you see the picture, 'the internet' stops being magic and becomes a chain you can name: browser → DNS → server → files → page.",
      def: "The web is a client-server system: browsers (clients) request files from machines (servers) found by name via DNS, and render what comes back.",
      code: "YOU:  type meenlearn.com + Enter\n\n1. BROWSER: 'who is meenlearn.com?'\n   DNS:     'that's 93.184.x.x'\n2. BROWSER -> SERVER at that address:\n   'GET / please'\n3. SERVER sends files back:\n   index.html + style.css + script.js\n4. BROWSER builds the page.\n\nYou:  a chain of 4 steps.\nEvery website: the same chain.\n\n'Client' = your browser.\n'Server' = the machine with the\nfiles. 'The web' = the exchange.",
      out: { web: { h: "<div style=\"font:13px/2 monospace;background:#14171f;color:#9fe8d8;padding:18px;border-radius:10px\">1. browser asks DNS: who is meenlearn.com?<br>2. DNS answers: 93.184.21.5<br>3. browser -> server: GET /index.html<br>4. server sends: html + css + js<br>5. browser paints the page<br><br><span style=\"color:#f5b23d\">You're looking at step 5.<br>Steps 1-4 already happened.</span></div>", c: "body{margin:0;padding:24px;background:#faf7f0}", j: "" } },
      flow: ["URL typed", "DNS: name -> address", "browser requests files", "server sends files", "browser paints the page"],
      task: "Open a page you use daily, open DevTools (F12) → Network tab, reload the page. Watch the list of files the server sent (html, css, js, images, fonts). How many files made up 'one page'? The web is more file-swapping than you thought.",
      mistake: "Thinking of 'a website' as one thing (a page you opened). It's actually dozens of files from one or more servers, assembled in your browser in milliseconds. When a page is 'half loaded', you're seeing that assembly mid-flight.",
      tip: "The client/server split is the idea that organizes everything else in this track: 'client' = your code in the browser (JS track), 'server' = where data and APIs live (Python/SQL worlds). Every deployment, API, and hosting decision is about which side of the split you're working on.",
      challenge: "You open a page and it shows content you typed into a form yesterday — but your site's code has no database. Explain where that data could have come from (two candidates: something stored in YOUR browser, or a server that remembers you). How would you tell which, using DevTools? (You already know one: localStorage. The other needs the Network tab.)" },

    { t: "What a URL is really saying",
      text: "A URL is a full address with a grammar: https://meenlearn.com/css#lesson-5. The scheme (https://) says which protocol to speak; the host (meenlearn.com) says which machine; the path (/css) says which file or page on it; the query (?x=1) carries extra data; the fragment (#lesson-5) points at a spot within the page. Read a URL like an address on an envelope — and you can predict what a server will do with it before you press Enter.",
      def: "URL anatomy: scheme://host/path?query#fragment — protocol, machine, file/route, extra data, in-page anchor.",
      code: "https://meenlearn.com/css?ref=home#l5\n |\n |  |                    |   |  |\n |  |                    |   |  + fragment\n |  |                    |   +   (in-page spot;\n |  |                    |      browser-only)\n |  |                    + query\n |  |                      (data for server)\n |  + path (/css = which page)\n +  host (which machine)\nhttps (protocol: secure HTTP)\n\nThe fragment never reaches the\nserver - it's a browser thing.\nQuery data DOES reach the server.",
      out: { term: "https://shop.example.com/products?id=7&sort=price#reviews\n\nscheme:  https\nhost:    shop.example.com\npath:    /products\nquery:   id=7&sort=price   -> the server sees\nfragment: #reviews         -> only the browser\n\nThe server builds a page from\npath+query; the browser scrolls\nto #reviews after it arrives." },
      flow: ["scheme (how)", "host (where)", "path (what)", "query (data)", "fragment (in-page spot)"],
      task: "Open five URLs from your browser history (or this site: the home page, a track page, a track with #l5). For each, label scheme/host/path/query/fragment out loud. Which parts did you see in the five, and which parts never appeared (and that's fine)?",
      mistake: "Assuming the #fragment is sent to the server (it isn't — it's stripped before the request leaves your browser; it exists only for scrolling). And assuming http:// and https:// are the same (https encrypts the conversation — a site without it can be tampered with; modern browsers mark http pages 'Not secure').",
      tip: "Query strings are how the web passes small data: ?page=2, ?q=shoes, ?id=7. Your JS (track 3) can read them with URLSearchParams — that's how 'shareable state' works (a link that reproduces a search). The URL is a data format, not just an address.",
      challenge: "Two users visit /products?id=7 — one gets a sneaker, the other gets a keyboard. The URL looks identical. Where could the difference come from (think: what else does the server know about each visitor — their stored session, their location, their account)? What does this tell you about 'the URL' being the whole story of a page request?" },

    { t: "DNS: from names to addresses",
      text: "Machines talk by number (IP addresses like 93.184.21.5); humans remember names (meenlearn.com). DNS (Domain Name System) is the translation layer — a global, distributed phone book. When your browser asks 'who is meenlearn.com?', DNS walks a chain of name servers (your ISP's, then the domain's registered ones) until it finds the IP. You can test the translation yourself: nslookup or dig from the terminal. It's the invisible first step of every page you've ever opened.",
      def: "DNS translates domain names to IP addresses via a hierarchical lookup — the internet's phone book, queried automatically before any request is sent.",
      code: "Machines:  93.184.21.5   (IP address)\nHumans:    meenlearn.com (name)\n\nDNS = the phone book between them.\n\nLookup chain:\n  your browser\n  -> your DNS resolver (ISP)\n  -> TLD server (.com 'who knows')\n  -> meenlearn's name servers\n  -> '93.184.21.5'\n\nTry it in the terminal:\n  nslookup meenlearn.com   (Windows)\n  dig meenlearn.com        (mac/Linux)\n\nThe name you bought is just an\nentry in this book - pointing at\nwherever your files live.",
      out: { term: "$ nslookup example.com\nServer:  resolver.local\nAddress: 192.168.x.x\n\nName:    example.com\nAddresses: 93.184.21.5\n          2606:2800:x::176\n\nAn A record = IPv4 number.\nAn AAAA = IPv6. A CNAME = 'that\nname really means THAT other\nname' (how hosting aliases work)." },
      flow: ["browser needs a name", "DNS resolver asks the chain", "chain finds the IP", "browser connects to the IP"],
      task: "In your terminal: nslookup (Windows) or dig (mac/Linux) on two sites you use daily. Compare their IP addresses — same host, different number, why? Now look up a site you KNOW is hosted on a big platform (a github.io site) — can you spot the pattern in its numbers?",
      mistake: "Believing a domain 'contains' a website (it doesn't — the domain is just a name in a phone book pointing at a machine; the files live on a server, and the name can point at different servers over time, which is how sites migrate without changing their address).",
      tip: "When a domain 'stops working' for everyone, the DNS is the first place to look: expired domain (no name servers), or a changed record (points at the wrong IP now). The diagnosis is one command: nslookup the domain and compare where it points vs where it should.",
      challenge: "You buy yourname.com and want it to show a site that actually lives on a host at 203.0.113.7. What EXACTLY do you change (what kind of DNS record, with what value), and why does the change take 'up to 48 hours' for the world to see (hint: the phone book is copied to many resolvers, and each copies on its own schedule — the name for that delay is TTL)?" },

    { t: "HTTP: the conversation, and status codes",
      text: "HTTP is the exact format of the browser↔server conversation. The request: a method (GET = fetch, POST = send data) + the path. The response: a status code + headers + the content. The status codes are the conversation's mood: 2xx = success (200 OK, 201 created), 3xx = redirect (301 moved permanently), 4xx = your problem (404 not found, 403 forbidden), 5xx = the server's problem (500 internal error). Learning the first-digit meanings turns any error page into a diagnosis: is it my fault or the machine's fault?",
      def: "HTTP request = method + path; response = status code + content. Codes: 2xx success, 3xx redirect, 4xx client error, 5xx server error.",
      code: "REQUEST (browser -> server):\n  GET /css HTTP/1.1\n  Host: meenlearn.com\n\nRESPONSE (server -> browser):\n  HTTP/1.1 200 OK\n  Content-Type: text/html\n  <html>...</html>\n\nSTATUS MOODS:\n  200 OK            all good\n  301 Moved         new home (redirect)\n  404 Not Found     no such thing\n  403 Forbidden     exists, but not\n                    for you\n  500 Error         the server broke\n\nFirst digit = who's at fault:\n4 = you (client), 5 = them (server).",
      out: { web: { h: "<p style=\"font-family:monospace;font-size:13px\">Try the buttons — each shows the status code the server would send:</p><button data-c=\"200\" data-m=\"OK — here's your page\">200</button> <button data-c=\"301\" data-m=\"Moved — go to the new address\">301</button> <button data-c=\"404\" data-m=\"Not found — no such thing\">404</button> <button data-c=\"500\" data-m=\"Server error — their problem\">500</button><pre id=\"o\" style=\"background:#14171f;color:#9fe8d8;padding:14px;border-radius:8px;font-size:12px\">HTTP/1.1 ? ?</pre>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222} button{padding:8px 14px;border:1px solid #ccc;border-radius:6px;background:#fff;cursor:pointer;font-weight:bold} #o{white-space:pre-wrap}", j: "document.querySelectorAll('button[data-c]').forEach(b=>b.addEventListener('click',()=>{document.getElementById('o').textContent='HTTP/1.1 '+b.dataset.c+' '+b.dataset.m+'\\n\\n'+(b.dataset.c[0]==='5'?'The server broke — retry or report it.':b.dataset.c[0]==='4'?'The request had a problem — check the address/permissions.':'Success or redirect — the web is working.');}));" } },
      flow: ["request: method + path", "server answers a code", "2xx ok / 3xx redirect", "4xx your problem / 5xx theirs"],
      task: "Use the demo buttons above, then do it for real: open DevTools → Network, visit a page, and find one 200 and (if you're feeling lucky) any non-200 in the list (broken images are usually 404s). For each, say the method (GET/POST) and the code. You're now reading the web's conversation directly.",
      mistake: "Assuming 404 always means 'the site is down' (it means 'this specific thing doesn't exist at this address' — the site is fine, the page isn't there) and assuming 500 means the internet is broken (it's ONE server's problem — retry, or it's their ticket to file).",
      tip: "The first-digit reflex: see a 4xx, check YOUR request (address, permissions, data); see a 5xx, retry and report (it's the machine's fault, not yours). That split saves hours of debugging in the wrong direction.",
      challenge: "A link you bookmarked now loads a different page but 'works' (no error). What status code explained that quietly (301/302 redirect — and where in DevTools can you SEE the redirect hop)? Why do websites redirect (moved homes, http→https, short links) instead of just 'breaking' the old address?" },

    { t: "HTTPS and the padlock: the web keeps its secrets",
      text: "That little lock (or the 'Not secure' warning) is HTTPS in action: the browser and server agree on an encrypted conversation so no one on the path — a café Wi-Fi, an ISP, a router — can read or alter what passes. Under the hood it's TLS with certificates: a trusted authority (a Certificate Authority) has checked that the server is who it claims to be, and the browser verifies that chain on every secure visit. For you: modern hosts (Pages, Netlify) issue these certificates automatically and free — you serve https, and the old http address redirects to it. And the flip side: a padlock does NOT make a site good. A fake bank page has a perfectly valid lock — so the habit is reading the address, not just the lock.",
      def: "HTTPS = HTTP encrypted with TLS: a verified certificate proves the server's identity and the conversation can't be read or tampered with in transit. Hosts issue free certificates automatically; the lock proves identity, not goodness.",
      code: "HTTP:  plain text over the wire\n  (anyone on the path can read it)\n\nHTTPS: HTTP inside an encrypted tube\n  (TLS) - the path sees only noise\n\nThe padlock = a TLS certificate:\n  a trusted authority checked\n  'this server is meenlearn.com'\n  and the browser verifies that\n  chain on every visit.\n\nFOR YOU:\n  Pages/Netlify: the certificate is\n  issued AUTOMATICALLY (free).\n  http://...  -> 301 -> https://...\n\nThe lock proves WHO the site is.\nIt does NOT prove the site is GOOD.\nRead the address. Every time.",
      out: { web: { h: "<p>Same page, two protocols — what the wire actually carries:</p><table style=\"font:12px monospace;border-collapse:collapse;width:100%;max-width:480px\"><tr style=\"background:#14171f;color:#f5b23d\"><th style=\"text-align:left;padding:8px\">request</th><th style=\"text-align:left;padding:8px\">what a passerby sees</th></tr><tr><td style=\"padding:8px;border-bottom:1px solid #eee\">http://bank/login</td><td style=\"padding:8px;border-bottom:1px solid #eee\">password=hunter2 (readable!)</td></tr><tr><td style=\"padding:8px\">https://bank/login</td><td style=\"padding:8px\">TLS handshake + encrypted bytes (noise)</td></tr></table><p style=\"font-size:12px;color:#777\">The padlock = a certificate the browser checked. http shows 'Not secure' in modern browsers — that's the warning, not the feature.</p>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222;margin:0}", j: "" } },
      flow: ["http = plain, readable", "https = TLS-encrypted", "certificate proves identity", "hosts: free, automatic"],
      task: "Open a site over https and click the padlock — read the certificate info (who issued it, which name it covers). Then type the same site with http:// and watch the 'Not secure' warning (and any redirect). Finally, find one site still without the padlock and explain what you'd do differently on it — and why.",
      mistake: "Treating the padlock as a quality seal (it's an identity + encryption check, not a 'good site' stamp — phishing sites wear perfectly valid locks) and treating http as 'just a little slower' (it's readable AND alterable by anyone on the path — logins can be stolen, pages injected with code).",
      tip: "The two-second habit: read the domain before typing anything sensitive (my-bank.com has a perfectly valid padlock). The lock answers 'is this really who they claim to be?'; your eyes answer 'is this who I meant to visit?'. Both are required.",
      challenge: "A visitor reports 'your site says Not secure', though it's on a host that offers free certificates. Walk the fix: confirm the certificate is actually enabled (view its info — auto-issuance sometimes needs a nudge or a little time), force the redirect so http always becomes https, and explain why that redirect is a 301 from the HTTP lesson doing a job you can now name. What breaks for a visitor who bookmarks the old http address if you skip the redirect?" },

    { t: "Hosting: what 'putting a site online' actually means",
      text: "Hosting = renting (or freeloading) a machine that's always on and reachable, where your files live so the world can request them. The options: static hosts for plain HTML/CSS/JS sites (GitHub Pages, Netlify — free tiers, ideal for everything you've built so far), shared/VPS servers when you need a backend (Python, SQL, APIs — the Python/SQL tracks' world), and the big platforms in between. For your current skills, 'hosting' means: a free static host, your files, a public URL. That's the whole job.",
      def: "Hosting is a always-on, publicly reachable machine (or service) that stores your site's files and serves them to browsers on request.",
      code: "Your files today: on YOUR computer.\n  -> reachable only by YOU.\n\nHosted: on a machine that's ON 24/7\n  and has a public address\n  -> reachable by EVERYONE.\n\nTHE OPTIONS:\n  Static (HTML/CSS/JS only):\n    GitHub Pages, Netlify  <- you are here\n  Server (needs a backend):\n    a VPS, a cloud service\n    (Python + SQL live here)\n\nFor everything this site taught\nso far: a free static host is\nthe whole deployment.",
      out: { term: "BEFORE:  your laptop (off at night,\n         private address)\nAFTER:   host machine (always on,\n         public address)\n\n  you  ->  https://you.host.com\n  friend -> https://you.host.com\n  stranger -> https://you.host.com\n\nSame files. New home. Public door.\nThat's hosting, in one picture." },
      flow: ["files on your machine", "copy them to a host", "host serves them 24/7", "public URL for everyone"],
      task: "Think about the last 'website' you published (even a school project). Where did its files live, and who kept that machine on? Now answer for your own work: of the 9 tracks you're learning, which ones produce files a STATIC host can serve, and which produce things that need a real server (hint: anything that runs code on request, or stores data)?",
      mistake: "Believing a static host can run your Python backend (it can't — static hosts serve FILES; they don't execute your code on each request. If your site needs a database or server logic, you need the server options — that's the Python/SQL world meeting hosting).",
      tip: "The decision is binary and simple: does my site do different things per visitor (data, accounts, submissions)? Yes → server hosting. No → static host, free tier, ship it. Most portfolios, docs, and landing pages are static — stop over-engineering the host.",
      challenge: "A friend's 'static' site remembers their quiz score between visits. No server, no database — how? (You built this exact trick: localStorage — the data lives in the BROWSER, not the host.) What's the trade-off of that trick (where does the score live if they use a different device)?" },

    { t: "Deploy with GitHub Pages: your first live site",
      needs: ["github", "git"],
      text: "GitHub Pages turns a public GitHub repo into a live website — free, forever, and perfect for everything you've built. The flow: put your site files in a repo → on GitHub, Settings → Pages → Source: main branch → Save. Seconds later your site is live at yourname.github.com/repo-name. From then on, every git push updates the live site automatically. This is the deployment path for every static project in this course — do it once, and 'publishing' is just pushing.",
      def: "GitHub Pages serves a public repo as a website at username.github.com/repo — enable it in repo Settings → Pages; pushes update the live site.",
      code: "THE FLOW (10 minutes):\n1. Local: your site folder = a git repo\n   (Terminal track: you know these)\n2. Push to GitHub (a PUBLIC repo):\n   git remote add origin <url>\n   git push -u origin main\n3. GitHub -> repo -> Settings -> Pages\n   Source: 'Deploy from a branch' -> main\n4. Wait ~1 min. It's live:\n   https://youname.github.com/repo\n\nAfter that:\n  edit locally -> git push\n  -> the live site updates.\n\nPublishing = pushing.",
      out: { term: "$ git push -u origin main\nEnumerating objects: 19, done.\n\n[on GitHub: Settings -> Pages\n Source: main / (root) -> Save]\n\n* Deploying from main branch...\n* Your site is live at:\n  https://youname.github.com/repo\n\n$ # later, after an edit:\n$ git commit -am \"Fix hero typo\"\n$ git push\n# live site: updated." },
      flow: ["site files in a repo", "push to GitHub (public)", "Settings → Pages → main", "live URL appears", "push = update"],
      task: "The real one: take ANY project you've built (or this course's site itself), make it a git repo, push to a public GitHub repo, enable Pages, and paste your live URL somewhere real (a message to a friend). Then make one small edit, push, and watch the live page change. You have officially been on the internet.",
      mistake: "Expecting Pages to work from a private repo without the right account plan (public repos are free; private needs a plan) and forgetting the index.html requirement (the root file the host serves must be named index.html — 'my site 404s' is usually 'my file isn't named index.html at the repo root').",
      tip: "The /repo-name in the URL is because Pages serves the repo as a subfolder. You can drop it (serve at the root of yourname.github.com) by naming the repo 'username.github.com' — a nice upgrade once you have a project worth a clean address.",
      challenge: "Your live Pages site links to style.css, and it 404s — the HTML looks right. Use DevTools → Network to see the exact URL the browser requested for the CSS, and compare it to the file's real location in the repo. Where's the mismatch (hint: relative paths meet a URL that includes /repo-name), and what's the robust fix for multi-page sites (hint: it's about paths from the site root)?" },

    { t: "Deploy with Netlify: drag, drop, done",
      needs: ["netlify"],
      text: "Netlify is the laziest free hosting that exists: open their site, drag your project folder onto the page, and it's live in about 30 seconds with a random URL (your-site-123.netlify.app). Connect a GitHub repo and every push auto-deploys — same workflow as Pages, different landlord. Why know two hosts? Because 'hosting' is a skill with options: different features (forms, functions, previews), different limits, and the ability to move your site without rewriting it. A portable site is a safe site.",
      def: "Netlify hosts static sites (and more) from a drag-and-drop folder or an auto-deploying Git connection — a second free option that keeps your hosting choice portable.",
      code: "TWO DOORS, SAME FILES:\n\n1. DRAG & DROP (30 seconds):\n   netlify.com -> log in ->\n   DRAG your folder onto the page\n   -> live at\n      random-name.netlify.app\n\n2. GIT CONNECT (continuous):\n   Netlify -> 'Add new site' ->\n   GitHub -> pick repo\n   -> every push auto-deploys\n   -> (bonus: a preview URL for\n      every branch / pull request)\n\nSame index.html as GitHub Pages.\nYour site is portable - the host\nis a landlord, not a cage.",
      out: { term: "1. netlify.com -> sign in (GitHub)\n2. drop the meenlearn/ folder\n   onto the dashboard\n3. ... deploying ...\n4. Live: https://meen-abc123.\n   netlify.app\n\n[edit a file, re-drop, or connect\n git and push -> updated]\n\nThe files didn't change. Only the\naddress did. That's portability." },
      flow: ["drag the folder (or connect git)", "Netlify copies + serves", "public netlify.app URL", "push = auto-update"],
      task: "Deploy the same site to BOTH GitHub Pages and Netlify (the files are identical — that's the point). Compare: same content, two addresses, two landlord dashboards. Then break one link on purpose, deploy to the other host, and watch it 'fix' itself by switching addresses. Hosting is a choice, not a commitment.",
      mistake: "Hardcoding your host's URL into the site (then you can't move without editing files) — keep links relative (/css.html, not https://myhost.netlify.app/css.html) and the site is host-agnostic. Relative paths are the portability habit.",
      tip: "Netlify's branch previews are the sleeper feature: connect a repo and every pushed branch gets its own preview URL. You (or a teammate) can review 'the new version' at a link before it's live. Free, and it changes how you test.",
      challenge: "Your site works at netlify.app but the GitHub Pages version looks broken (missing CSS — the path problem from the last lesson, again). Diagnose with DevTools → Network: what's the failed URL, what's the real file path, and which single change to the HTML (the path style) fixes BOTH hosts at once? (You're learning the 'relative-to-root' pattern that makes sites host-proof.)" },

    { t: "Custom domains: yourname.com",
      text: "A custom domain is buying a name (yourname.com — a few dollars a year from a registrar like Namecheap or Cloudflare) and pointing it at your host. The pointing is a DNS record: an A record (your domain → the host's IP) or a CNAME (your domain → the host's address, e.g. yourname.github.io). Both hosts (Pages and Netlify) tell you exactly which record to add. Five minutes of DNS, and your work lives at yourname.com instead of a subdomain.",
      def: "A custom domain = a purchased name pointed at your host via DNS records (A or CNAME) — your work at yourname.com.",
      code: "THE UPGRADE:\n  youname.github.io/repo\n        becomes\n  yourname.com\n\nHOW:\n1. Buy the name (a registrar:\n   Namecheap, Cloudflare, ...)\n2. At your HOST, 'custom domain'\n   settings -> they give you the\n   record to add:\n     A     yourname.com  -> host IP\n     CNAME www.yourname.com -> host\n3. Add it at the REGISTRAR's DNS\n   page (the phone book - lesson 3)\n4. Wait for DNS to spread (minutes\n   to hours)\n5. (usually) add HTTPS - the hosts\n   issue the certificate for free\n\nThe name is yours. The files are\nthe host's. DNS joins them.",
      out: { term: "BEFORE:\n  https://youname.github.io/site\n\nDNS change at the registrar:\n  A   yourname.com   185.199.108.153\n  CNAME www.yourname.com\n        youname.github.io\n\nAFTER (once DNS spreads):\n  https://yourname.com   <- live\n  (HTTPS certificate:\n   issued automatically by the host)\n\nSame files. Same repo. New name." },
      flow: ["buy the name", "host tells you the record", "add it at the registrar", "DNS spreads (TTL)", "https://yourname.com live"],
      task: "Paper-deploy it: for your live Pages/Netlify site, write out the exact two DNS records you'd add (look up your host's 'custom domain' docs for the real values — don't add them unless you actually bought the name). Then explain in one sentence why the change 'propagates' over time instead of happening instantly (TTL — lesson 3's term, now applied).",
      mistake: "Adding the record at the host instead of the registrar (the phone book is edited at the REGISTRAR — the host only TELLs you what to write) and expecting instant results (TTL: resolvers worldwide hold copies; the spread takes minutes to hours, and 'it works on my machine but not my friend's' during propagation is normal, not a bug).",
      tip: "Always set up both the bare domain (yourname.com) AND www (www.yourname.com) — visitors type both. The standard move: point both at the site and redirect one to the other, so there's exactly one canonical address (which also keeps your SEO clean).",
      challenge: "Your domain works at yourname.com but www.yourname.com shows the host's default page. What record is missing (the CNAME for www), and after you add it, why might you still see the old behavior for a while (two reasons: TTL, and your browser's own DNS cache — what's the nuclear option for the browser cache)? (You're now debugging the full chain: browser → resolver → phone book → host.)" },

    { t: "Static vs dynamic: the line that decides your architecture",
      text: "Static site: the host sends the same files to everyone, every time — your HTML/CSS/JS, pre-built. Dynamic site: the server runs code on each request and builds the answer — a database query, a logged-in user's name, today's date. The line between them decides your hosting, your backend needs, and your complexity budget. Most beginner projects should be static (faster, cheaper, simpler, safer) — go dynamic only when the site genuinely must do different things per visitor. (Modern note: 'static' sites get dynamic-feeling features from client-side JS + APIs — you've already built that pattern.)",
      def: "Static = same files served to all; dynamic = server runs code per request (databases, accounts). Choose static until the site must differ per visitor.",
      code: "STATIC (you are here):\n  browser requests /css.html\n  host sends the FILE css.html\n  same file, every visitor, always\n\nDYNAMIC:\n  browser requests /dashboard\n  server RUNS code:\n    who's logged in?  -> Ada\n    query the DB      -> Ada's rows\n    build the page    -> Ada's page\n  different answer per visitor\n\nThe line: 'does the answer depend\non the visitor or the moment?'\n  no  -> static (ship it)\n  yes -> dynamic (server + DB)",
      out: { web: { h: "<p>Same question asked two ways — watch the 'server' answer:</p><button id=\"s\">Static request</button> <button id=\"d\">Dynamic request</button><pre id=\"o\" style=\"background:#14171f;color:#9fe8d8;padding:14px;border-radius:8px;font-size:12px;min-height:70px\">pick one</pre>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222} button{padding:8px 14px;border:1px solid #ccc;border-radius:6px;background:#fff;cursor:pointer;font-weight:bold} #o{white-space:pre-wrap}", j: "document.getElementById('s').addEventListener('click',()=>{document.getElementById('o').textContent='GET /about.html\\nhost: opens file about.html\\nsends the SAME bytes\\nto every visitor, ever.\\n(no code ran)'});document.getElementById('d').addEventListener('click',()=>{document.getElementById('o').textContent='GET /dashboard\\nserver: who is logged in? -> Ada\\n  queries DB for Ada\\n  BUILDS Ada-specific html\\n(a different answer per visitor)'});" } },
      flow: ["static: file out", "dynamic: code runs, answer built", "the question: per-visitor?", "static until it must not be"],
      task: "Classify five sites you use daily as static or dynamic (a news site's article page? a login page? a portfolio? a shopping cart? a weather page?). For each, say the ONE thing that puts it on its side (what's the same for everyone vs what depends on the visitor/moment). Your JS track's to-do app is a third category — say which side it leans on and why.",
      mistake: "Reaching for a database and a backend for a site that's 95% static (a portfolio that 'remembers' a theme setting needs localStorage, not a server — the to-do pattern you already built) and, conversely, trying to build per-user features with static files alone (you can't — that's what the server is for).",
      tip: "The modern sweet spot: a static site + a few APIs (the JS track's fetch lesson). Your page is fast and free to host; the parts that genuinely need a backend (accounts, submissions) are small API calls to a service. 'Mostly static, surgically dynamic' is how real products are built.",
      challenge: "A blog is static. The owner wants a comment form. Option A: a real backend + database. Option B: a third-party comment service (an API). Option C: 'save comments to localStorage'. For each option, say who the data belongs to, what breaks first, and which option a solo blogger should actually pick (and why C is a trap despite being free)?" },

    { t: "Forms and the server: where data goes",
      text: "A form's action URL and method decide where submitted data travels: action=\"/signup\" method=\"post\" sends the fields to that URL on the server. On a static host, there's no server to receive it — which is why static forms either call an API (a form-backend service, or your own endpoint) or use the service's own widget. The JS-track lesson handled the client side (preventDefault, read values); this is the destination question: data leaves the browser for a server, and something on the other end must be ready to receive it.",
      def: "A form's action+method send its data to a URL on a server; static sites need an API or a form service as the destination.",
      code: "<form action=\"/signup\" method=\"post\">\n\naction  = WHERE the data goes\nmethod  = post (sends data) /\n          get (data in the URL)\n\nSTATIC HOST? no /signup exists.\nOptions:\n  1. a form service (their endpoint)\n  2. YOUR api (Node/Python - the\n     backend tracks)\n  3. a JS fetch to an endpoint\n     (JS lesson 13's pattern)\n\nThe browser side you already built.\nThis is the door it walks through.",
      out: { term: "form submits ->\n  POST /signup\n  name=Ada&email=ada@x.com\n\nserver receives, validates,\nsaves (SQL track's world),\nanswers (201 Created / error).\n\nStatic site? the 'server' is an\nAPI or a form service -\nsame conversation, different\nlandlord." },
      flow: ["form: action + method", "browser sends the fields", "server receives + validates", "saves + answers"],
      task: "Take your JS-track form (the one with preventDefault). Trace its lifecycle out loud: what happens on submit, what you read, where the data COULD go (list the three destinations from this lesson). Then, on paper, write the form's action+method that would send it to a real endpoint at https://api.example.com/signup — and say which part of that sentence the browser would strip (hint: you can't cross-origin a plain form the way you can a fetch — why does fetch give you more control?).",
      mistake: "Assuming action=\"/signup\" works on a static host (there's no /signup there — the submission 404s or reloads weirdly) and using method=\"get\" for sensitive data (GET puts the data in the URL — visible in history, logs, and the address bar; POST is for sending data, GET is for requesting).",
      tip: "The mental upgrade: a form is a letter with an address (action) and a delivery method (method). The JS track addressed the writing; this lesson addresses the mailing. Both halves matter — a perfect letter to a wrong address still doesn't arrive.",
      challenge: "A static site's contact form 'works' — submissions arrive in the owner's inbox. No backend, no database. Name the two most likely mechanisms (a form service's endpoint behind a fetch, or the service's hosted widget) and how you'd tell which from the page source (what would you see in the HTML/JS — an action URL? a script tag from a third party?). What's the privacy trade-off of each?" },

    { t: "APIs: talking to other apps' data",
      text: "An API is a program-to-program door: a URL that answers with data when you ask the right way. You've already used the client side (JS lesson 18: fetch → res.json()). Here's the picture: public APIs expose data (weather, news, exchanges), you request a specific endpoint (GET https://api.example.com/users/7), and you get structured data back (JSON). The skill: read an API's documentation like a manual — base URL, endpoints, what each returns, any key needed — and call it from your code. Every 'live data' feature you've ever seen is this pattern.",
      def: "An API is a set of URLs (endpoints) that return structured data (usually JSON) in response to correctly-formatted requests — the web's program-to-program interface.",
      code: "YOU (a program) -> AN API (a service)\n\nGET https://api.example.com/users/7\n\n<-\n{\n  \"id\": 7,\n  \"name\": \"Ada\",\n  \"role\": \"admin\"\n}\n\nREADING API DOCS = the skill:\n  base URL?  endpoints?\n  GET or POST? auth (key)?\n  what shape is the answer?\n\nYou built the client (fetch +\nres.json). Now you can call\nANYONE'S data.",
      out: { web: { h: "<p>Watch a real public API answer (no key needed):</p><button id=\"g\" style=\"padding:9px 16px;border:0;border-radius:8px;background:#14171f;color:#f5b23d;font-weight:bold;cursor:pointer\">fetch a user from the public API</button><pre id=\"o\" style=\"background:#14171f;color:#9fe8d8;padding:14px;border-radius:8px;font-size:12px;white-space:pre-wrap;min-height:60px\">ready…</pre>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222}", j: "document.getElementById('g').addEventListener('click',async()=>{const o=document.getElementById('o');o.textContent='GET https://jsonplaceholder.typicode.com/users/1 ...';try{const r=await fetch('https://jsonplaceholder.typicode.com/users/1');const u=await r.json();o.textContent='HTTP '+r.status+' ->\\n'+JSON.stringify({id:u.id,name:u.name,company:u.company.name},null,2)+'\\n\\n(no network in the sandbox? that\\u2019s the caught path - real browsers show the JSON)';}catch(e){o.style.color='#e2664f';o.textContent='caught: '+e.message;}});" } },
      flow: ["read the API docs", "request the right endpoint", "get JSON back", "use it in your app"],
      task: "In a real browser: open DevTools → Console, run fetch('https://jsonplaceholder.typicode.com/users/1').then(r=>r.json()).then(u=>console.log(u.name)) — a real public API, one line, live data in your console. Then find one free public API you care about (weather, books, cats), read its docs for the base URL + one endpoint, and call it. You've now talked to the internet like a program.",
      mistake: "Guessing endpoints from memory instead of reading the docs (the docs are the contract — base URL, path, method, auth, response shape; a wrong guess gives a 404 that looks mysterious until you read) and assuming every API needs a key (many public ones don't for basic use; when they do, the docs say where to get one — and see lesson 17 for where the key lives).",
      tip: "The docs-reading order that works every time: (1) base URL, (2) one simple GET endpoint, (3) its example response, (4) auth. Get #1-3 working before you think about #4. You can read any API manual with this sequence — it's a format, not a mystery.",
      challenge: "An API docs page shows: GET /weather?city=lagos returns {temp: 31, unit: 'c'}, and 'requires header X-Api-Key'. Decode the full request you'd send (the URL with its query, the method, the header) — and say exactly where the API key lives in that picture (which part of the request, not your code file — lesson 17's rule applies). What status code would you get if you forgot the header (and why that code specifically)?" },

    { t: "JSON: the data language of the web",
      text: "JSON (JavaScript Object Notation) is plain text shaped like your objects and arrays: {\"name\": \"Ada\", \"age\": 36, \"skills\": [\"css\", \"sql\"]}. It's the web's exchange format because any language can read it. You've met it on three sides already: JavaScript (it's native objects), Python (json.loads / json.dumps), and fetch (res.json()). Master the shape — objects, arrays, strings, numbers, booleans, null — and you can read any API response, any config file, any data file on the internet.",
      def: "JSON is a text format for objects/arrays/primitives, readable by every language — the standard exchange format for APIs and data files.",
      code: "JSON (text):            JS (native):\n{\n  \"name\": \"Ada\",    {\n  \"age\": 36,        name: \"Ada\",\n  \"admin\": true,    age: 36,\n  \"skills\": [\"css\"]  admin: true,\n}                   skills: [\"css\"]\n                  }\nSame shape. One is TEXT,\none is a live object.\n\nThe 6 types: object {}, array [],\nstring \"\", number, true/false, null\n\nPython: json.loads(text) / json.dumps(obj)\nJS:     JSON.parse(text)  / JSON.stringify\nYou've met JSON on all three sides.",
      out: { web: { h: "<p>The same data, three languages, one shape:</p><pre id=\"o\" style=\"background:#14171f;color:#9fe8d8;padding:14px;border-radius:8px;font-size:12px;white-space:pre-wrap\">{ \"name\": \"Ada\", \"age\": 36 }</pre>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222}", j: "const js={name:'Ada',age:36};const text=JSON.stringify(js,null,2);document.getElementById('o').textContent='JS object:  {name: \"Ada\", age: 36}\\n-> JSON text:  '+text+'\\n-> back to JS:  '+JSON.stringify(JSON.parse(text))+'\\n\\nparse/stringify = the door between\\nthe live object and the text.';" } },
      flow: ["objects/arrays as TEXT", "every language reads it", "parse -> use -> stringify", "APIs, configs, data files"],
      task: "Hand-write a valid JSON object describing yourself (name, age, skills array, a nested 'learning' object with this site's tracks). Paste it into a JSON validator (or a browser console: JSON.parse(yourText)) and confirm it parses. Now break it one way (a trailing comma) and read the error. JSON is strict — feel the strictness.",
      mistake: "Trailing commas and single quotes (JSON is stricter than JS objects: no trailing commas, double quotes only — a file that 'looks like JSON' and fails to parse is almost always one of these two) and confusing JSON (text) with an object (live data) — you can't send an object over the wire; you stringify it first, and the receiver parses it.",
      tip: "The two-function habit: JSON.stringify to LEAVE (form data, fetch bodies, localStorage), JSON.parse to ENTER (API responses, loaded files). Every time data crosses a boundary (screen ↔ wire ↔ disk), it passes through one of these two doors.",
      challenge: "A config file is valid in JavaScript but invalid as JSON. Find the two likely culprits (unquoted keys, a trailing comma — or a comment; JSON has none). Then answer the design question: why did the web pick a STRICT format (no comments, no trailing commas) over a looser one? (Hint: strictness is what lets any language parse it with zero ambiguity — and the file is often written by machines, not humans.)" },

    { t: "CORS and why 'it works locally but not online'",
      text: "CORS (Cross-Origin Resource Sharing) is the browser's rule: a page may only read data from servers that explicitly allow it. Your page at http://localhost:8000 asking api.example.com is a cross-origin request — the API must answer with a header (Access-Control-Allow-Origin) naming your origin, or the browser blocks the result (even though the request reached the server and got an answer!). That's why a fetch that works in one context fails in another — and why 'it works in the playground but not on my page' is usually CORS, not your code.",
      def: "CORS is a browser security policy: cross-origin fetch results are blocked unless the server sends Access-Control-Allow-Origin permitting the requesting origin.",
      code: "Your page:  http://localhost:8000\nAPI:         https://api.example.com\n\nDifferent ORIGIN (scheme+host+port)\n-> the browser asks the API:\n   'will you let localhost read you?'\n\nAPI answers with a header:\n  Access-Control-Allow-Origin:\n  http://localhost:8000   <- allowed!\n  (or * for 'anyone')\n\nNo header / wrong origin ->\n  the BROWSER blocks the result.\nThe server DID answer - the\ndoor was the browser, not the net.",
      out: { web: { h: "<p>Same fetch, two origins — watch which one the 'browser' allows:</p><button id=\"a\" style=\"padding:9px 14px;border:0;border-radius:8px;background:#14171f;color:#f5b23d;font-weight:bold;cursor:pointer\">Server allows my origin</button> <button id=\"b\" style=\"padding:9px 14px;border:0;border-radius:8px;background:#5E8CE6;color:#fff;font-weight:bold;cursor:pointer;margin-left:8px\">Server allows nobody</button><pre id=\"o\" style=\"background:#14171f;color:#9fe8d8;padding:14px;border-radius:8px;font-size:12px;min-height:60px\">pick one</pre>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222}", j: "const show=(t,c)=>{const o=document.getElementById('o');o.style.color=c;o.textContent=t;};document.getElementById('a').addEventListener('click',()=>show('request -> api.example.com\\nresponse header: Access-Control-Allow-Origin: mysite.com\\nbrowser: origin matches -> DATA DELIVERED', '#7ee0a0'));document.getElementById('b').addEventListener('click',()=>show('request -> api.example.com\\nresponse: (no allow header)\\nbrowser: cross-origin, not permitted\\n-> blocked. The data ARRIVED and was refused at the door.', '#ff9d8a'));" } },
      flow: ["cross-origin request", "server's allow header?", "yes -> browser delivers", "no -> browser blocks it"],
      task: "Use the demo above to feel the two outcomes. Then, in a real browser: open a page on one site and try fetching a different site's API from its console (e.g. on a news site, fetch a weather API). Read the console error word for word — it names CORS. You've now caught the bug in the wild and can read its name.",
      mistake: "Concluding 'the API is down' when the real message is 'the browser refused to hand you the answer' (the network tab shows the request SUCCEEDED — the block happens after, in the browser; that split is the whole diagnosis) and 'fixing' CORS by moving your page to the same server as the API (that's one fix — but the normal fix is on the API side, or using a same-origin proxy; the API must cooperate, or you must route around it).",
      tip: "The 30-second CORS diagnosis: (1) Network tab — did the request get a 2xx? (2) Console — does it say 'blocked by CORS'? Both yes = the server didn't allow your origin. If you control the API: send the header. If you don't: ask the API docs whether they support CORS / need a key that implies it.",
      challenge: "Your app works at localhost during development, but deployed to yoursite.com it fails on the same API call. The API owner says 'I only allow localhost'. Explain the fix in the owner's terms (allow yoursite.com too — the header can list it; or * for public read-only APIs) and why 'just allow localhost' was a development shortcut that bit you in production. What's the safest default for a PUBLIC read-only API, and for a PRIVATE one with accounts (and why they differ)?" },

    { t: "Node.js: JavaScript off the browser",
      needs: ["node"],
      text: "Node.js is the JavaScript engine (the same one your browser uses) running on your machine or a server — no DOM, no window, just the language plus superpowers: fs (files), http (serve pages!), process (the machine). This is why the web's tooling is JavaScript: the same language writes the browser app AND the server that feeds it. You already ran it in the Terminal track; here's the why: one language, two homes, and the backend world of Python/SQL gets a JS twin.",
      def: "Node.js runs JavaScript outside the browser, adding server-side tools (fs, http) — the same language for frontend and backend, and the engine behind the web's tooling.",
      code: "BROWSER JS:  document, window, fetch\nNODE JS:      fs (files), http (servers),\n              process, + all your JS\n\nSame language. Different home.\n\nThe tiny server (server.js):\n  const http = require(\"http\");\n  http.createServer((req, res) => {\n    res.end(\"hello from my server\");\n  }).listen(3000);\n\nRun: node server.js\nOpen: http://localhost:3000\nYOU ARE NOW A SERVER.",
      out: { term: "$ cat > server.js <<'EOF'\nconst http = require('http');\nhttp.createServer((req, res) => {\n  res.end('hello from MY server');\n}).listen(3000);\nEOF\n\n$ node server.js\n# (in another window / the browser)\n$ curl http://localhost:3000\nhello from MY server\n\nYour machine just served the web.\nThat's Node." },
      flow: ["same JS language", "new home: the machine", "fs + http superpowers", "you serve pages yourself"],
      task: "If Node is installed: write the tiny server from the lesson (or paste it), run node server.js, open http://localhost:3000 in your browser, and read your own server's greeting. Then curl it from the terminal. You have personally been both client and server — the split from lesson 1, lived. If Node isn't installed, write the server down as your 'first Node day' script.",
      mistake: "Writing browser code in Node and being confused when document is undefined (different environment — no DOM; the file system and http module are the substitutes), and forgetting the server process is still running in your terminal (Ctrl+C stops it; 'port already in use' on the next run means the old one is still holding it).",
      tip: "The unification to feel: the JS track taught you the LANGUAGE; Node gives it a body outside the browser. When a tutorial says 'express', 'middleware', or 'REST endpoint', it's Node wearing a costume — the variables, functions, and fetch-shaped thinking are all already yours.",
      challenge: "Extend your tiny server: make it answer / with 'home' and /api with a JSON object (res.setHeader('Content-Type','application/json'); res.end(JSON.stringify({ok:true}))). Visit both URLs. You've just built the two halves of a real app (a page route + a data route) — what's the name for the second half (an API endpoint), and which lesson of this track does it connect to?" },

    { t: "npm and packages: sharing code on the web",
      needs: ["node"],
      text: "npm (bundled with Node) is JavaScript's package registry — millions of open-source packages, installable in one command: npm install express. npm init -y creates a package.json (your project's identity: name, version, dependencies); node_modules/ is where installed packages live (never commit it — it's regenerable, exactly like .venv); and npm run <script> runs the commands you define. The pattern mirrors pip/venv from the Python track: registry + manifest + local install folder. You're not learning a new idea — you're meeting the same idea in another language's house.",
      def: "npm installs JavaScript packages from a public registry; package.json is the project manifest; node_modules holds installs (never committed); npm run executes defined scripts.",
      code: "npm init -y           # create package.json\nnpm install express   # add a package\n  -> lands in node_modules/\n  -> recorded in package.json\n\nnpm run dev           # run a script\n  (you define scripts in package.json:\n   \"scripts\": { \"dev\": \"node app.js\" })\n\nTHE PATTERN (same as pip/venv):\n  registry  -> manifest -> local folder\n  npm       -> package.json -> node_modules\n  pypi      -> requirements -> .venv\n\nnode_modules: NEVER commit.\n.gitignore it. Anyone can\nrebuild it from package.json.",
      out: { term: "$ npm init -y\nWrote /project/package.json\n\n$ npm install express\nadded 64 packages in 3s\n\n$ ls\nnode_modules  package.json  package-lock.json\n\n$ cat .gitignore\nnode_modules/\n\nThe manifest + lockfile travel.\nThe folder regenerates. (Same\ncontract as Python's venv.)" },
      flow: ["npm init (manifest)", "npm install (packages in)", "node_modules (local, ignored)", "npm run (your scripts)"],
      task: "In a scratch folder: npm init -y, npm install a small real package (npm install chalk is a classic — it colors terminal text), write a two-line script that uses it (require('chalk').green('hello')), run it with node, and confirm node_modules appears. Then write the .gitignore line. You've now completed the full package lifecycle in the JS world.",
      mistake: "Committing node_modules (it's huge and regenerable — the 'why is my repo 2GB' bug; the manifest is the source of truth, the folder is a build artifact — same rule as .venv, Git lesson 12's .gitignore) and installing global packages for project needs (npm install -g is for tools you run everywhere; project packages belong in the project's node_modules).",
      tip: "package.json's scripts section is the project's instruction card: 'npm run dev' does the same thing for everyone on the team — no more 'what command does this project use?' Slack messages. When you join any JS project, read package.json first: it's the front door.",
      challenge: "Two developers both work on a JS project. A adds express, B adds chalk, both push. The merge of package.json is clean, but B's node_modules doesn't have express (and vice versa). What's the fix (one command, and it works for both), and what's the file that makes 'npm install with no arguments' rebuild the EXACT versions everyone tested with (the lockfile — what does it pin, and why is that pin a feature)?" },

    { t: "Environment variables and secrets: never commit a key",
      text: "Secrets (API keys, passwords, tokens) must never live in your code — committed code is history, and history is forever (and public on GitHub). The pattern: put secrets in a .env file (KEY=value lines), add .env to .gitignore (Git lesson 12), and load them at runtime (process.env.API_KEY in Node, os.environ in Python). If a secret ever gets committed: rotate it FIRST (make the old key dead), then clean the history — a leaked key is a live risk until it's revoked, no matter how clean your git log looks afterwards.",
      def: "Secrets live in environment variables (.env files, gitignored, loaded at runtime) — never in committed code; a leaked secret is rotated immediately, then purged from history.",
      code: "BAD (committed, public, forever):\n  const key = \"sk-ant-abc123...\";\n\nGOOD:\n  # .env            <- gitignored!\n  API_KEY=sk-ant-abc123...\n\n  // code\n  const key = process.env.API_KEY;  (Node)\n  key = os.environ[\"API_KEY\"]       (Python)\n\nIF IT EVER GOT COMMITTED:\n  1. ROTATE the key NOW (old key =\n     dead. This is step ONE.)\n  2. THEN clean history (filter-repo)\n  3. force-push + tell teammates\n\nA leaked key is live until revoked.\nHistory cleanup is cosmetic;\nrotation is the medicine.",
      out: { term: "# .env\nAPI_KEY=sk-test-123\nDB_PASS=correct-horse\n\n$ grep API_KEY .env\nAPI_KEY=sk-test-123\n\n$ git status\n# .env: untracked (gitignored -\n#   it will NEVER be committed)\n\n$ git check-ignore .env\n.env     <- proof it's ignored" },
      flow: ["secret -> .env (local only)", ".env -> .gitignore", "code reads process.env", "leak? ROTATE first, clean second"],
      task: "The full ritual, in a scratch repo: create a .env with a fake key, add it to .gitignore, confirm with git check-ignore .env, write code that reads it (Node or Python — your choice), and commit everything EXCEPT the .env. Then the disaster drill: on a copy, commit the .env by accident, run git log --all to 'find' it in history, and walk through the rotation-first response out loud. You've now handled a real incident on paper.",
      mistake: "Thinking 'I'll just remove the file in a later commit' undoes the leak (it doesn't — the key lives in every commit between; history is append-only, and public repos are crawled by key-scanners within minutes) and storing secrets in code 'just for now' (the 'just for now' is how every famous leak starts — the habit is the point).",
      tip: "The two-check habit: before git add ., glance for .env (or run git status and read the untracked list); before pushing, confirm the new files don't include secrets. Two seconds each, and they've saved careers. (Tools exist — pre-commit secret scanners — but the glance is faster.)",
      challenge: "A public repo's commit history contains a real database password, discovered three weeks ago. Sequence the response exactly: (1) what's the FIRST action and why it beats every git command, (2) what to do about the history, (3) what to tell the team, (4) what process change prevents the repeat (the gitignored .env + the pre-push check + maybe a scanner). Which step do panicked teams usually skip, and what's the cost of skipping it?" },

    { t: "Performance: pages that load fast",
      text: "Users leave slow pages — and 'fast' is mostly three habits: small (compress images, trim code), few (each file is a round trip — combine where sensible), and close (host where users are — CDNs do this for you). The biggest win is almost always images: a 2MB photo where a 200KB one works is 90% of a slow page. Then: modern formats (WEBP), lazy-loading below-the-fold images (you met this in the HTML track), fonts with display:swap, and minified CSS/JS. Check your own page in DevTools → Network: sort by size, and you can see the wins available to you.",
      def: "Web performance = small files (images first), fewer requests, close hosting (CDN) — measured by sorting your page's Network tab by size.",
      code: "THE BIG THREE:\n  SMALL  images (the usual 80%!),\n         modern formats (WEBP),\n         minified css/js\n  FEW    every file = a round trip;\n         combine, cache, lazy-load\n  CLOSE  CDN = your files served\n         from near each visitor\n\nMEASURE (DevTools -> Network):\n  reload, sort by SIZE\n  what's the biggest file?\n  (bet: an image, or a font)\n\nLazy-load (HTML lesson 16):\n  <img loading=\"lazy\"> - below the\n  fold waits until you scroll\n\nFast is a habit, not a math problem.",
      out: { web: { h: "<p>Your page's 'Network tab', sorted by size — the diagnosis in one view:</p><table style=\"font:12px monospace;border-collapse:collapse;width:100%;max-width:460px\"><tr style=\"background:#14171f;color:#f5b23d\"><th style=\"text-align:left;padding:8px\">file</th><th style=\"text-align:right;padding:8px\">size</th><th style=\"text-align:left;padding:8px\">the fix</th></tr><tr><td style=\"padding:8px;border-bottom:1px solid #eee\">hero-photo.jpg</td><td style=\"text-align:right;padding:8px;border-bottom:1px solid #eee\">1.9 MB</td><td style=\"padding:8px;border-bottom:1px solid #eee\">WEBP + resize → 180 KB</td></tr><tr><td style=\"padding:8px;border-bottom:1px solid #eee\">font.woff2</td><td style=\"text-align:right;padding:8px;border-bottom:1px solid #eee\">96 KB</td><td style=\"padding:8px;border-bottom:1px solid #eee\">swap + subset</td></tr><tr><td style=\"padding:8px\">style.css</td><td style=\"text-align:right;padding:8px\">48 KB</td><td style=\"padding:8px\">minify → 12 KB</td></tr></table><p style=\"font-size:12px;color:#777\">Total before: 2.0 MB. After the fixes: ~200 KB. Same page, ten times lighter.</p>", c: "body{font-family:sans-serif;background:#faf7f0;padding:24px;color:#222;margin:0}", j: "" } },
      flow: ["open Network tab", "sort by size", "fix the big ones (images!)", "fewer + closer", "fast is a habit"],
      task: "The real audit: open this site (or any page you built) in DevTools → Network, reload, sort by size. List the three biggest files and name the fix for each (image format/resize, font swap, minify). Then check your total transfer size — and do the same audit on a 'slow' site you've noticed lagging. Can you see WHY it's slow now?",
      mistake: "Optimizing code before images (the code is usually 5% of the weight; the photos are 80% — fix the biggest number first, always) and shipping a 4000px-wide hero photo to fit a 800px slot (resize to the DISPLAY size; 'it looks the same' is exactly the problem — the bandwidth doesn't know that).",
      tip: "The one-command feel: after any change, check the Network tab's total size — you can 'see' performance improve (2.0 MB → 200 KB) instead of trusting that you did. Measured beats intended, on the web as everywhere else in this course.",
      challenge: "A page is fast on your machine but slow for a visitor across the ocean. Your Network tab looks fine. What's missing from your diagnosis (the CLOSE half of the trio — the round-trip distance; a CDN serves files from a machine near the visitor), and how would you confirm it (compare your load times vs theirs, or a tool that loads the page from their region)? What's the fix, and which part of it is 'just using a host that has a CDN' vs 'real work'?" },

    { t: "Going live: the real-world deploy checklist",
      text: "Before you publish anything real, the checklist that separates 'it worked on my machine' from 'it works for everyone': (1) the site is complete and validated (HTML track's validator), (2) all links and images resolve (click every link; check the Network tab for 404s), (3) it looks right at mobile width (resize, or DevTools device mode — CSS lesson 14), (4) the meta tags are set (title, description, viewport — HTML lesson 17), (5) it's on git with a clean log (Git track), (6) pushed, deployed, and the LIVE URL tested in a different browser or device than the one you built on. Six checks, ten minutes, and 'deploy day' stops being a nervous event.",
      def: "The pre-launch checklist: valid + complete, links/images verified, mobile-tested, meta set, git-clean, deployed and tested on the live URL from a different device.",
      code: "THE CHECKLIST (10 minutes):\n[ ] validated, no errors (HTML track)\n[ ] every link clicked; Network tab\n    shows no 404s\n[ ] mobile width: looks right\n    (DevTools device mode)\n[ ] meta: title, description,\n    viewport (HTML lesson 17)\n[ ] git: committed, clean log\n    (the history reads like a story)\n[ ] deployed -> test the LIVE URL\n    on ANOTHER browser/device\n\n'my machine' is not 'the internet'.\nShip from the checklist, not the\nvibe.",
      out: { term: "$ git status        # clean?\n$ git log --oneline # reads well?\n$ git push\n\n[deploy: Pages / Netlify]\n\n$ # in ANOTHER browser:\n$ # open the live URL ->\n   works. links work. mobile works.\n\nLive. Checked. Boring - in the best\npossible way." },
      flow: ["validate", "links + 404s", "mobile width", "meta tags", "git clean + push", "test the LIVE URL elsewhere"],
      task: "Run the full checklist on a real project of yours (or this course's site itself), out loud, check by check. Find at least one thing it catches that you'd have shipped without noticing (almost everyone finds one — a dead link, a missing meta, a mobile squish). That one find is the checklist's entire value, paid for in ten minutes.",
      mistake: "Testing only on the machine you built on (the cache, the extensions, and the 'it looks fine here' all lie to you — the live URL in a different browser/device is the truth) and deploying from an uncommitted state (if the live site has a bug and your local folder doesn't match it, you can't reproduce or fix it — git clean BEFORE deploy, always).",
      tip: "Make the checklist a file in the project (CHECKLIST.md) and tick it each time — ten lines of markdown that have caught more launch-day bugs than any amount of testing. The habit compounds: every boring, checked deploy builds the confidence for the next one.",
      challenge: "A project passes the full checklist, deploys fine, and then one visitor reports 'the page is blank for me'. Your live URL looks perfect everywhere you try. Walk the diagnosis: what's different about THEIR machine (browser? region? network? a cached broken file from an earlier deploy?), what's the one question to ask them (what do you see in DevTools → Network / Console?), and why 'works for me' + 'blank for them' points at the cache or their connection before anything in your code? (You're now debugging the full chain: your code → the host → the network → their browser.)" },

    { t: "Project: publish a real site from machine to internet",
      needs: ["git", "github"],
      opt: ["vscode", "node"],
      text: "The capstone, end to end: take a real project (build one small if you need — a personal page is perfect), run the deploy checklist, and put it on the internet with a live URL you can send to a person. The full chain, every track meeting at once: the code (HTML/CSS/JS tracks), the terminal (running, committing, pushing), git (the clean history), and a host (Pages or Netlify). When you send someone that link and they open it from THEIR machine — that's the moment 'my computer' becomes 'the web'. This is the finish line of the course.",
      def: "Publish a real site end to end: build → checklist → git clean → deploy (Pages or Netlify) → test the live URL from another device → share it. Every track, one final chain.",
      code: "THE FULL CHAIN (all tracks, once):\n\n1. BUILD   a real page (HTML/CSS/JS)\n2. CHECK   the lesson-19 checklist\n3. TERMINAL  git init/add/commit -\n    clean log (Terminal + Git tracks)\n4. PUSH    to a public GitHub repo\n    (or Netlify drag-drop)\n5. HOST    Pages: Settings->Pages->main\n    (or the Netlify dashboard)\n6. PROVE   open the LIVE URL on\n    another browser/device\n7. SHARE   send it to a real person\n\nYou started this course typing\n'echo hello'. Now the internet\nhas your work on it. Ship it.",
      out: { term: "$ git log --oneline\na1b2c3d Add contact form\n9f8e7d6 Add about section\n5a4b3c2 Initial structure\n\n$ git push -u origin main\n$ # [Settings -> Pages -> main]\n\nLive: https://you.github.io/site\n\n$ # friend's phone, their network:\n$ # opens it. works.\n\n...from 'echo hello' to the\ninternet. That's the whole course." },
      flow: ["build it", "checklist", "git clean + push", "enable the host", "test the live URL elsewhere", "send it to a person"],
      task: "Do it — for real, today. Build or pick a page, run the checklist, push, deploy, open the live URL on your phone (a genuinely different device), and send the link to one human being. Screenshot the moment it opens on their screen. That screenshot is the certificate this course can't issue any better.",
      mistake: "Stopping at 'it's deployed' without the cross-device test (the checklist's last item is the whole point — your laptop's cache has been lying to you this whole session) and treating the URL as disposable (write it down, put it in your README/profile — a live site you never share is a practice site; the sharing is what makes it real).",
      tip: "The loop you've now built is the professional loop, at beginner scale: build → check → version → deploy → verify live → share → get feedback → build again. Every product, every career in this field, is that loop turned more times. You own the loop now.",
      challenge: "One week after launch, you have an idea for a big new section. Walk the full professional cycle without breaking the live site: branch (feature/section), build there, checklist ON THE BRANCH, preview (Netlify's branch preview, or Pages on a branch), merge, push, verify live. Where does each tool from each track appear in that sentence — and why is 'branch first' what keeps the live site safe while you experiment? (Git lesson 9, wearing its working clothes.)" }
  ],

  quizzes: {
    terminal: [
      { q: "What does pwd do?", a: ["Prints the date", "Prints your current folder", "Opens a folder in the GUI", "Deletes a file"], c: 1, w: "pwd (print working directory) tells you where you're standing — the first thing to type when lost." },
      { q: "cd .. moves you…", a: ["Into a folder named ..", "Up one level (to the parent folder)", "To the root", "Home"], c: 1, w: ".. is the parent folder; cd .. walks out one level. (~ is home, / is root.)" },
      { q: "Which command deletes a file — with no undo?", a: ["mv", "cp", "rm", "del-file"], c: 2, w: "rm removes permanently — no trash bin, no are-you-sure. ls before every rm." },
      { q: "The difference between > and >> when writing files is…", a: ["No difference", "> overwrites the file, >> appends to the end", ">> overwrites, > appends", "> is for folders"], c: 1, w: "One bracket replaces the whole file (destructive); two brackets add to the end." },
      { q: "'command not found' usually means…", a: ["The computer is broken", "The program isn't installed or isn't on PATH", "You typed too fast", "The terminal is full"], c: 1, w: "The terminal couldn't find the program in any folder on PATH — install it, or fix PATH (and open a new terminal)." },
      { q: "In the command git commit -m \"Add index\", what is -m?", a: ["An argument (the file)", "A flag (tweaks behavior)", "The program", "A path"], c: 1, w: "Flags start with - and tweak behavior (-m means 'the message follows'); arguments are the things acted on." },
      { q: "What does the pipe (|) do?", a: ["Creates a pipe file", "Sends one command's output into the next command", "Runs commands in parallel", "Pauses the terminal"], c: 1, w: "cat log.txt | grep error | wc -l — each | hands its output to the next command's input." },
      { q: "You're stuck inside less viewing a big file. Which key quits?", a: ["Esc", "q", "x", "Ctrl+D"], c: 1, w: "q quits less. (Ctrl+C stops running commands generally; q is less's own quit key.)" }
    ],
    web: [
      { q: "DNS translates…", a: ["IP addresses to names", "Names (domains) to IP addresses", "URLs to file names", "HTML to CSS"], c: 1, w: "DNS is the internet's phone book: meenlearn.com → 93.184.x.x, looked up before any request is sent." },
      { q: "A 500 status code means…", a: ["Your request was wrong", "The server itself had an error", "The page moved", "The file is missing"], c: 1, w: "5xx = the server's fault (retry or report). 4xx = the client's fault (check the request). First digit = who's at fault." },
      { q: "The #fragment part of a URL is…", a: ["Sent to the server", "Used only by the browser (in-page spot)", "The password", "The server name"], c: 1, w: "The fragment never leaves your browser — it's for scrolling to a spot within the page; query data (?x=1) DOES reach the server." },
      { q: "GitHub Pages serves…", a: ["Any code, including backends", "A public repo as a static website", "Only images", "Databases"], c: 1, w: "Pages turns a public repo into a live static site at username.github.com/repo — enable in Settings → Pages; pushes update it." },
      { q: "A custom domain works by…", a: ["Renaming the host's folder", "Pointing your bought name at the host via DNS records (A/CNAME)", "Editing the HTML", "Paying the browser"], c: 1, w: "The domain is a phone-book entry (at your registrar) pointing at your host's address — the files don't move, the name does." },
      { q: "A static site vs a dynamic site: the deciding question is…", a: ["How many pages it has", "Does the answer differ per visitor or moment?", "Which language it's in", "Whether it has a database"], c: 1, w: "Same files for everyone = static (fast, free to host). Server runs code per request (accounts, data) = dynamic." },
      { q: "A fetch works locally but fails on your deployed site, and the Network tab shows the request succeeded. The likely cause is…", a: ["The API is down", "CORS — the browser blocked the cross-origin result", "Your HTML is invalid", "The server is too slow"], c: 1, w: "The request reached the server; the BROWSER refused to hand you the answer because the server didn't send an Access-Control-Allow-Origin for your origin." },
      { q: "An API key must NEVER be…", a: ["In a .env file", "In environment variables", "Committed in your code", "Rotated if leaked"], c: 2, w: "Committed code is public history forever. Keys live in gitignored .env files / environment variables — and if leaked, rotate FIRST, then clean history." }
    ]
  }
};
